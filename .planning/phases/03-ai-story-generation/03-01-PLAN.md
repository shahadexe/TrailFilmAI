---
phase: 03-ai-story-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/gemini/client.ts
  - src/lib/gemini/prompts.ts
  - src/lib/gemini/generateStory.ts
  - src/types/gemini.ts
  - .env.example
autonomous: true
requirements:
  - STORY-02
  - STORY-03
user_setup:
  - service: gemini
    why: "Required for the Gemini multimodal API call that generates story chapters"
    env_vars:
      - name: GEMINI_API_KEY
        source: "https://aistudio.google.com/app/apikey — sign in with Google account, click 'Create API key', copy the key"
        target_file: ".env.local"
        note: "MUST be present in .env.local (not just .env.example). client.ts throws at module load if missing, which means `pnpm dev` will fail with a clear error before serving any request. `pnpm build` does NOT catch this because the Gemini code path is only reached at runtime. The user must add `GEMINI_API_KEY=<your-key>` to `.env.local` before running `pnpm dev`."
    dashboard_config: []

must_haves:
  truths:
    - "buildStoryPrompt(title, destination, photos, tone) returns a prompt string containing all 4 tone instructions when called with that tone"
    - "STORY_TONES exports the 4 keys: cinematic, poetic, adventurous, documentary"
    - "generateStory() fetches each photo URL, base64-encodes via loop (never spread), calls gemini-2.5-flash with responseMimeType application/json, parses the JSON, returns a GeneratedChapter[] of length 3-6"
    - "The prompt contains the D-06 anti-slop directive: 'Describe only what you actually observe in each photo'"
    - "Module is Edge-Runtime safe: zero imports of node:* / Buffer / fs / path"
    - "client.ts throws a clear error at module load time if GEMINI_API_KEY is missing or empty (fail-fast — surfaces in `pnpm dev` immediately, not at first request)"
  artifacts:
    - path: "src/lib/gemini/client.ts"
      provides: "Shared GoogleGenerativeAI instance singleton"
      exports: ["genAI"]
    - path: "src/lib/gemini/prompts.ts"
      provides: "buildStoryPrompt() and STORY_TONES export"
      exports: ["buildStoryPrompt", "STORY_TONES"]
    - path: "src/lib/gemini/generateStory.ts"
      provides: "generateStory(tripTitle, destination, photos, tone): Promise<GeneratedChapter[]>"
      exports: ["generateStory", "arrayBufferToBase64"]
      contains: "for (let i = 0; i < bytes.byteLength; i++)"
    - path: "src/types/gemini.ts"
      provides: "PhotoInput and GeneratedChapter types"
      exports: ["PhotoInput", "GeneratedChapter"]
    - path: ".env.example"
      provides: "GEMINI_API_KEY entry (already present — verify)"
      contains: "GEMINI_API_KEY"
  key_links:
    - from: "src/lib/gemini/generateStory.ts"
      to: "src/lib/gemini/prompts.ts"
      via: "import { buildStoryPrompt } from './prompts'"
      pattern: "buildStoryPrompt\\("
    - from: "src/lib/gemini/generateStory.ts"
      to: "src/lib/gemini/client.ts"
      via: "import { genAI } from './client'"
      pattern: "genAI\\.getGenerativeModel"

threat_model_notes:
  trust_boundary: "Module is server-only — Gemini API key must NEVER reach the client bundle"
  asvs_l1_relevant: ["V5 Input Validation (tone)", "V6 Cryptography (none)"]
---

<objective>
Create the Gemini integration library — a pure module of three TypeScript files plus a shared types file — that wraps `@google/generative-ai` v0.24.1, builds the prompt for all four tones with the D-06 anti-slop directive, and exposes a single `generateStory()` entry point that fetches photos from public URLs, base64-encodes them safely for Edge Runtime, calls `gemini-2.5-flash` with JSON-forced output, and returns the parsed chapters array.

Purpose: This is the most critical file in the codebase per Build Guide §9 ("80% of output quality depends on this prompt"). The library is consumed by `/api/generate-story/route.ts` (Plan 03-03) and must be Edge-Runtime safe (no `Buffer`, no Node.js APIs).

Output: Four new files under `src/lib/gemini/` and `src/types/gemini.ts`, plus verification that `.env.example` declares `GEMINI_API_KEY` AND that `.env.local` contains a non-empty `GEMINI_API_KEY=<key>` line. No UI, no API route, no DB writes in this plan — those come in Plan 03-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-story-generation/03-CONTEXT.md
@.planning/phases/03-ai-story-generation/03-RESEARCH.md
@.planning/phases/03-ai-story-generation/03-UI-SPEC.md
@Ideation/02_MVP_BUILD_GUIDE.md

<interfaces>
<!-- Existing types this plan consumes. -->

From src/types/database.ts:
```typescript
export type StoryTone = 'cinematic' | 'poetic' | 'adventurous' | 'documentary'
```

From node_modules/@google/generative-ai (v0.24.1, Edge-safe ESM):
```typescript
import { GoogleGenerativeAI } from '@google/generative-ai'
const genAI = new GoogleGenerativeAI(apiKey: string)
const model = genAI.getGenerativeModel({ model: string, generationConfig: { temperature?: number, responseMimeType?: string } })
await model.generateContent([prompt: string, ...parts: { inlineData: { data: string, mimeType: string } }[]])
// result.response.text() → string (JSON when responseMimeType is application/json)
```

Existing .env.example already contains:
```
GEMINI_API_KEY=
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create gemini types and client singleton (with fail-fast env guard)</name>
  <files>src/types/gemini.ts, src/lib/gemini/client.ts</files>
  <read_first>
    - src/types/database.ts (StoryTone type — re-use, do NOT re-declare)
    - Ideation/02_MVP_BUILD_GUIDE.md §9 (PhotoMeta / GeneratedChapter shape — adapt to PhotoInput per RESEARCH.md Pattern 1)
    - .planning/phases/03-ai-story-generation/03-RESEARCH.md "Standard Stack" + "Pattern 3" (genAI singleton pattern)
    - .env.example (verify GEMINI_API_KEY entry already exists; do NOT add duplicate)
    - .env.local (verify GEMINI_API_KEY=<non-empty-value> line is present BEFORE writing code — surface this requirement to the user if missing)
  </read_first>
  <behavior>
    - Test 1: src/types/gemini.ts exports `PhotoInput` interface with fields `id: string, url: string, takenAt?: string, latitude?: number, longitude?: number, locationName?: string`
    - Test 2: src/types/gemini.ts exports `GeneratedChapter` interface with fields `title: string, narrative: string, photoIndices: number[], locationName: string | null`
    - Test 3: src/lib/gemini/client.ts exports `genAI` initialised with `process.env.GEMINI_API_KEY!`
    - Test 4: src/lib/gemini/client.ts has an explicit guard at module top: `if (!process.env.GEMINI_API_KEY) throw new Error('GEMINI_API_KEY is required')` — fails fast at module load if the env var is undefined or empty string
    - Test 5: `.env.local` contains a non-empty `GEMINI_API_KEY=<value>` line (verified via shell check; if missing, user is instructed to add it before `pnpm dev`)
  </behavior>
  <action>
    Create `src/types/gemini.ts` with two exported interfaces — `PhotoInput` and `GeneratedChapter` — exactly as described in the behavior block. Import nothing; these are pure type declarations. Do NOT re-declare `StoryTone` — that already lives in `src/types/database.ts`.

    Create `src/lib/gemini/client.ts` that:
    1. Imports `GoogleGenerativeAI` from `@google/generative-ai`.
    2. Reads `process.env.GEMINI_API_KEY` into a const. At the top of the module, add an EXPLICIT guard: `if (!process.env.GEMINI_API_KEY) { throw new Error('GEMINI_API_KEY is required') }`. This is an eager check at module load time — `pnpm dev` will fail immediately with a clear error if the user has not configured `.env.local`. (Per checker feedback BLOCKER 1.)
    3. Exports a `const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY)` singleton.

    Use `export const genAI` (not default export). Do NOT import any Node.js modules (no `fs`, `path`, `buffer`). The file is consumed by the Edge Runtime route in Plan 03-03.

    Verify `.env.example` already contains `GEMINI_API_KEY=` (it does per the Read file). Do not duplicate the line. If it is missing, add it as `GEMINI_API_KEY=` with no value.

    **User-setup gate (CRITICAL — surface before implementation):** Before writing code, run `grep -E "^GEMINI_API_KEY=.+" .env.local` to confirm the user has set the key. If the gate fails (exit code non-zero), STOP and prompt the user: "Add GEMINI_API_KEY=<your-key> to .env.local (get a key at https://aistudio.google.com/app/apikey) before continuing. pnpm dev will fail at module load without it." Do not proceed until the user confirms or the grep returns a match.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/types/gemini.ts` exists; `grep -E "export interface (PhotoInput|GeneratedChapter)" src/types/gemini.ts` returns 2 matches.
    - File `src/lib/gemini/client.ts` exists; `grep -E "export const genAI" src/lib/gemini/client.ts` returns 1 match.
    - `grep -E "process\.env\.GEMINI_API_KEY" src/lib/gemini/client.ts` returns ≥ 1 match.
    - `grep -F "if (!process.env.GEMINI_API_KEY)" src/lib/gemini/client.ts` returns 1 match (eager guard at module load — per checker BLOCKER 1).
    - `grep -F "GEMINI_API_KEY is required" src/lib/gemini/client.ts` returns 1 match (error message).
    - `grep -E "from 'buffer'|from 'fs'|from 'path'|from 'node:" src/lib/gemini/client.ts src/types/gemini.ts` returns 0 matches.
    - `grep -E "^GEMINI_API_KEY=" .env.example` returns 1 match (already present).
    - `grep -E "^GEMINI_API_KEY=.+" .env.local` returns exit 0 with a non-empty value (per checker BLOCKER 1 — `.env.local` MUST contain the key, or `pnpm dev` will fail at module load). If this grep fails, the task is INCOMPLETE — instruct user to populate `.env.local` from https://aistudio.google.com/app/apikey before resuming.
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>Type definitions and client singleton exist, the module fails fast at load time when GEMINI_API_KEY is missing (eager guard, not just non-null assertion), `.env.local` is verified to contain the key, no Node.js APIs are imported, and the project still type-checks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create prompts.ts with STORY_TONES and buildStoryPrompt (anti-slop directive included)</name>
  <files>src/lib/gemini/prompts.ts</files>
  <read_first>
    - Ideation/02_MVP_BUILD_GUIDE.md §9 (the full `STORY_TONES` object and `buildStoryPrompt` skeleton — copy the structure verbatim, then adapt per the changes below)
    - .planning/phases/03-ai-story-generation/03-RESEARCH.md "Code Examples — buildStoryPrompt" (the modified version that includes D-06 rule #9)
    - .planning/phases/03-ai-story-generation/03-CONTEXT.md D-06 (verbatim anti-slop directive)
    - src/types/database.ts (StoryTone)
    - src/types/gemini.ts (PhotoInput — created in Task 1)
  </read_first>
  <behavior>
    - Test 1: `STORY_TONES.cinematic.label === 'Cinematic'`, `.description === 'Like a Nat Geo documentary'`, `.instruction` mentions 'National Geographic'
    - Test 2: `STORY_TONES.poetic.description === 'Lyrical and reflective'`
    - Test 3: `STORY_TONES.adventurous.description === 'Bold and energetic'`
    - Test 4: `STORY_TONES.documentary.description === 'Observational and grounded'`
    - Test 5: `buildStoryPrompt('Goa', null, [], 'cinematic')` returns a string containing 'TRIP TITLE: "Goa"', 'TONE: Cinematic', and the anti-slop rule #9
    - Test 6: The prompt contains all 9 OUTPUT RULES (numbered 1-9), and rule 9 is the anti-slop directive from D-06
    - Test 7: The prompt contains the OUTPUT FORMAT block with the JSON schema `{ "chapters": [ { "title", "narrative", "photoIndices", "locationName" } ] }`
  </behavior>
  <action>
    Create `src/lib/gemini/prompts.ts`. Structure:

    1. Import `StoryTone` from `@/types/database` (do NOT re-export — single source of truth).
    2. Import `PhotoInput` from `@/types/gemini`.
    3. Export `STORY_TONES` as `const` (use `as const` for literal narrowing). Use the EXACT 4-tone object from Build Guide §9 (cinematic, poetic, adventurous, documentary) with the same `label`, `description`, and `instruction` strings. The descriptions must match the UI-SPEC copy contract verbatim:
       - cinematic: "Like a Nat Geo documentary"
       - poetic: "Lyrical and reflective"
       - adventurous: "Bold and energetic"
       - documentary: "Observational and grounded"
    4. Export `buildStoryPrompt(tripTitle: string, destination: string | null, photos: PhotoInput[], tone: StoryTone): string` returning a template-literal prompt with this structure (preserve order and exact wording — these are calibrated):
       - Lead: `You are a world-class travel storyteller writing a cinematic journal of a user's trip${destination ? \` to ${destination}\` : ''}.`
       - `TRIP TITLE: "${tripTitle}"`
       - `NUMBER OF PHOTOS: ${photos.length}`
       - `TONE: ${STORY_TONES[tone].label}`
       - `TONE INSTRUCTION:` + the tone's `instruction`
       - `PHOTO METADATA (in order):` followed by one line per photo: `Photo ${i + 1}: ${p.locationName || 'Unknown location'}${p.takenAt ? \`, taken ${p.takenAt}\` : ''}${p.latitude && p.longitude ? \` (${p.latitude.toFixed(3)}, ${p.longitude.toFixed(3)})\` : ''}`
       - `YOUR TASK:` paragraph (copy verbatim from Build Guide §9)
       - `OUTPUT RULES:` block with exactly 9 numbered rules. Rules 1-8 are verbatim from Build Guide §9. Rule 9 is the D-06 anti-slop directive: `9. Describe only what you actually observe in each photo — specific colors, subjects, moments. Never generalize about a type of place or write as if you haven't seen the image.`
       - `OUTPUT FORMAT — return ONLY valid JSON, no markdown, no preamble:` followed by the JSON schema example block.

    Do NOT re-declare `StoryTone` here — import from `@/types/database`. Do NOT add any Buffer/Node imports.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/gemini/prompts.ts` exists.
    - `grep -E "export const STORY_TONES" src/lib/gemini/prompts.ts` returns 1 match.
    - `grep -E "export function buildStoryPrompt" src/lib/gemini/prompts.ts` returns 1 match.
    - `grep -c "cinematic\|poetic\|adventurous\|documentary" src/lib/gemini/prompts.ts` returns ≥ 4 (each tone key appears in STORY_TONES).
    - `grep -F "Like a Nat Geo documentary" src/lib/gemini/prompts.ts` returns 1 match.
    - `grep -F "Lyrical and reflective" src/lib/gemini/prompts.ts` returns 1 match.
    - `grep -F "Bold and energetic" src/lib/gemini/prompts.ts` returns 1 match.
    - `grep -F "Observational and grounded" src/lib/gemini/prompts.ts` returns 1 match.
    - `grep -F "Describe only what you actually observe" src/lib/gemini/prompts.ts` returns 1 match (D-06 anti-slop directive present verbatim).
    - `grep -F 'OUTPUT FORMAT' src/lib/gemini/prompts.ts` returns 1 match.
    - `grep -F 'photoIndices' src/lib/gemini/prompts.ts` returns ≥ 1 match (JSON schema example).
    - `grep -E "from 'buffer'|from 'fs'|from 'path'|from 'node:" src/lib/gemini/prompts.ts` returns 0 matches.
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>The prompt file is complete: 4 tone definitions with correct labels/descriptions/instructions, `buildStoryPrompt` returns the full multi-section prompt including the 9 output rules (rule 9 = D-06 anti-slop directive), no Node.js APIs imported.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create generateStory.ts with Edge-safe base64 loop and gemini-2.5-flash call</name>
  <files>src/lib/gemini/generateStory.ts</files>
  <read_first>
    - .planning/phases/03-ai-story-generation/03-RESEARCH.md "Pattern 1: Edge Runtime Base64 Encoding" (the loop-based arrayBufferToBase64 implementation — copy verbatim)
    - .planning/phases/03-ai-story-generation/03-RESEARCH.md "Pattern 3: Gemini Multimodal Call" (SDK usage)
    - .planning/phases/03-ai-story-generation/03-RESEARCH.md "Pitfall 1" (RangeError on btoa spread — verify the loop approach is used, NEVER the spread)
    - .planning/phases/03-ai-story-generation/03-RESEARCH.md "Assumption A3" + suggested fix (MIME type detection from Content-Type response header — do NOT hardcode image/jpeg)
    - .planning/phases/03-ai-story-generation/03-CONTEXT.md D-01 (gemini-2.5-flash NOT 2.0-flash NOT 1.5-flash)
    - src/lib/gemini/client.ts (genAI singleton from Task 1)
    - src/lib/gemini/prompts.ts (buildStoryPrompt from Task 2)
    - src/types/gemini.ts (PhotoInput, GeneratedChapter)
    - src/types/database.ts (StoryTone)
  </read_first>
  <behavior>
    - Test 1: `arrayBufferToBase64(buffer)` uses a `for (let i = 0; i < bytes.byteLength; i++)` loop with `binary += String.fromCharCode(bytes[i])` — never the spread operator
    - Test 2: `generateStory(title, dest, photos, tone)` calls `genAI.getGenerativeModel({ model: 'gemini-2.5-flash', generationConfig: { temperature: 0.8, responseMimeType: 'application/json' } })`
    - Test 3: For each photo, the function calls `fetch(photo.url)`, reads `Content-Type` header, falls back to 'image/jpeg' if missing, and produces an inlineData part with the detected mimeType (per A3 suggested fix)
    - Test 4: The function calls `model.generateContent([prompt, ...imageParts])` and then `JSON.parse(result.response.text())`
    - Test 5: The function returns the `chapters` array from the parsed response (typed as `GeneratedChapter[]`)
    - Test 6: If JSON.parse throws OR `parsed.chapters` is not an array OR has length outside 3-6 OR any item is missing `title`/`narrative`/`photoIndices`, the function throws (caller in Plan 03-03 handles the retry per D-05)
    - Test 7: No `Buffer.from` anywhere in the file — only `btoa` + `Uint8Array` loop
  </behavior>
  <action>
    Create `src/lib/gemini/generateStory.ts`:

    1. Import `genAI` from `./client`.
    2. Import `buildStoryPrompt` from `./prompts`.
    3. Import `PhotoInput`, `GeneratedChapter` from `@/types/gemini`.
    4. Import `StoryTone` from `@/types/database`.

    Export `arrayBufferToBase64(buffer: ArrayBuffer): string`. Implementation MUST use the loop pattern from RESEARCH Pattern 1 (verbatim):
    ```
    const bytes = new Uint8Array(buffer)
    let binary = ''
    for (let i = 0; i < bytes.byteLength; i++) {
      binary += String.fromCharCode(bytes[i])
    }
    return btoa(binary)
    ```
    NEVER use `btoa(String.fromCharCode(...new Uint8Array(buffer)))` — that throws RangeError on images >60KB. (See RESEARCH Pitfall 1.)

    Export `async function generateStory(tripTitle: string, destination: string | null, photos: PhotoInput[], tone: StoryTone): Promise<GeneratedChapter[]>` that:

    1. Builds `imageParts` by `Promise.all(photos.map(async (p) => { ... }))` where for each photo:
       - `const res = await fetch(p.url)`
       - `const mimeType = res.headers.get('content-type')?.split(';')[0] ?? 'image/jpeg'` (per RESEARCH A3 fix — handles JPEG/PNG/WEBP from Phase 2 uploads)
       - `const buffer = await res.arrayBuffer()`
       - `const data = arrayBufferToBase64(buffer)`
       - returns `{ inlineData: { data, mimeType } }`
    2. Calls `genAI.getGenerativeModel({ model: 'gemini-2.5-flash', generationConfig: { temperature: 0.8, responseMimeType: 'application/json' } })` — use the literal string `'gemini-2.5-flash'`, NOT `'gemini-2.0-flash'` (retires June 1, 2026 per RESEARCH OQ-01) and NOT `'gemini-1.5-flash'` (Build Guide is outdated).
    3. Builds the prompt: `const prompt = buildStoryPrompt(tripTitle, destination, photos, tone)`.
    4. Calls `const result = await model.generateContent([prompt, ...imageParts])`.
    5. Parses: `const text = result.response.text(); const parsed = JSON.parse(text) as { chapters?: GeneratedChapter[] }`.
    6. Validates: if `!Array.isArray(parsed.chapters)` → throw `new Error('Gemini response missing chapters array')`. If `parsed.chapters.length < 3 || parsed.chapters.length > 6` → throw `new Error(\`Gemini returned ${parsed.chapters.length} chapters (expected 3-6)\`)`. For each chapter, validate `typeof c.title === 'string' && typeof c.narrative === 'string' && Array.isArray(c.photoIndices)` — throw if any check fails.
    7. Returns `parsed.chapters`.

    Do NOT swallow errors here — the caller in Plan 03-03 implements the D-05 retry. Do NOT import `Buffer` or any Node.js module.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/gemini/generateStory.ts` exists.
    - `grep -E "export function arrayBufferToBase64" src/lib/gemini/generateStory.ts` returns 1 match.
    - `grep -E "export async function generateStory" src/lib/gemini/generateStory.ts` returns 1 match.
    - `grep -E "for \(let i = 0; i < bytes\.byteLength; i\+\+\)" src/lib/gemini/generateStory.ts` returns 1 match (loop pattern present).
    - `grep -E "binary \+= String\.fromCharCode\(bytes\[i\]\)" src/lib/gemini/generateStory.ts` returns 1 match.
    - `grep -F "...new Uint8Array" src/lib/gemini/generateStory.ts` returns 0 matches (spread NOT used).
    - `grep -F "Buffer.from" src/lib/gemini/generateStory.ts` returns 0 matches.
    - `grep -F "'gemini-2.5-flash'" src/lib/gemini/generateStory.ts` returns 1 match.
    - `grep -E "'gemini-2\.0-flash'|'gemini-1\.5-flash'" src/lib/gemini/generateStory.ts` returns 0 matches.
    - `grep -F "responseMimeType: 'application/json'" src/lib/gemini/generateStory.ts` returns 1 match.
    - `grep -F "res.headers.get('content-type')" src/lib/gemini/generateStory.ts` returns 1 match (MIME detection per A3).
    - `grep -F "Gemini response missing chapters array" src/lib/gemini/generateStory.ts` returns 1 match (validation present).
    - `grep -E "from 'buffer'|from 'fs'|from 'path'|from 'node:" src/lib/gemini/generateStory.ts` returns 0 matches.
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>generateStory.ts is created with the Edge-safe loop-based base64, gemini-2.5-flash model, JSON response mode, MIME type detection from response header, chapter count validation (3-6), and zero Node.js imports. The library is ready for Plan 03-03 to wire into the API route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process.env → client bundle | GEMINI_API_KEY must remain server-only — never prefixed with NEXT_PUBLIC_ |
| photo URL → fetch → Gemini | Photos are public Supabase Storage URLs owned by the same user (RLS verified upstream in Plan 03-03 route) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | src/lib/gemini/client.ts | mitigate | Use `process.env.GEMINI_API_KEY` (no NEXT_PUBLIC_ prefix); library is only imported by server/Edge code; verified via grep that no client component imports `@/lib/gemini/*` (enforced in Plan 03-03 acceptance criteria) |
| T-03-02 | Tampering | src/lib/gemini/generateStory.ts | mitigate | Validate `parsed.chapters` is an array, length 3-6, each item has required fields. Caller in Plan 03-03 handles retry on parse/validation failure (D-05) |
| T-03-03 | Denial of Service | src/lib/gemini/generateStory.ts | accept | No timeout wrapper here — caller (Edge route) is bounded by Vercel 300s edge runtime cap; Gemini free tier has 15 RPM ceiling per RESEARCH |
| T-03-04 | Tampering (prompt injection via title/destination) | src/lib/gemini/prompts.ts | accept | Template literal interpolation is safe (not SQL); Gemini structured output (responseMimeType: application/json) constrains response shape; risk is low — malicious title could only influence prose, not exfil data |
</threat_model>

<verification>
**Build verification (per task):** `pnpm build` exits 0 after each task.

**Phase-level (after all 3 tasks):**
- `grep -rE "from '@/lib/gemini'" src/` returns 0 matches (nothing imports the lib yet — Plan 03-03 will).
- `grep -rE "from 'buffer'|Buffer\.from|from 'node:" src/lib/gemini/ src/types/gemini.ts` returns 0 matches (Edge-safe verified).
- `grep -rE "process\.env\.GEMINI_API_KEY" src/lib/gemini/` returns exactly 1 file match (only client.ts reads it).
- `grep -rF "NEXT_PUBLIC_GEMINI" src/` returns 0 matches (key is never exposed to client).
- `grep -E "^GEMINI_API_KEY=.+" .env.local` returns exit 0 (per checker BLOCKER 1 — `.env.local` MUST contain a non-empty key; pnpm dev will crash at module load otherwise).
</verification>

<success_criteria>
- All 4 files exist: `src/types/gemini.ts`, `src/lib/gemini/client.ts`, `src/lib/gemini/prompts.ts`, `src/lib/gemini/generateStory.ts`.
- `pnpm build` exits 0.
- `generateStory()` is exported and typed as `(tripTitle: string, destination: string | null, photos: PhotoInput[], tone: StoryTone) => Promise<GeneratedChapter[]>`.
- Zero Node.js imports anywhere in the new files.
- `gemini-2.5-flash` is the only model string in the codebase.
- D-06 anti-slop directive is verbatim in the prompt as rule #9.
- `.env.example` declares `GEMINI_API_KEY` (was already true before this plan; verified not duplicated).
- `.env.local` declares `GEMINI_API_KEY=<non-empty-value>` (verified before code is written — `pnpm dev` would crash at module load without it).
- `client.ts` contains an eager `if (!process.env.GEMINI_API_KEY) throw new Error(...)` guard (per checker BLOCKER 1).
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-story-generation/03-01-SUMMARY.md` with:
- Files created (4)
- Public surface (functions exported, their signatures)
- Confirmation that gemini-2.5-flash is the configured model
- Confirmation that the btoa loop pattern is in use (no spread operator)
- Confirmation that `.env.local` contains GEMINI_API_KEY and that client.ts has the fail-fast guard
- Any deviations from the plan
- Next: Plan 03-02 (store + StepIndicator + globals.css) runs in parallel with this plan in Wave 1; Plan 03-03 (API route + Wizard Step 3) consumes this library in Wave 2.
</output>
</output>
