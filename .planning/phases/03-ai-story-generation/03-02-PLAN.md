---
phase: 03-ai-story-generation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/trip-creation.ts
  - src/components/upload/StepIndicator.tsx
  - src/app/globals.css
autonomous: true
requirements:
  - STORY-01

must_haves:
  truths:
    - "The Zustand store accepts step values 1, 2, AND 3 — TypeScript narrows to the literal union `1 | 2 | 3`"
    - "The store exposes `selectedTone: StoryTone` (default 'cinematic') and `setSelectedTone` action"
    - "StepIndicator renders three step nodes (Details / Photos / Tone) and announces 'Step X of 3' via aria-label"
    - "globals.css defines the `.progress-indeterminate` animation + class with a reduced-motion guard"
  artifacts:
    - path: "src/stores/trip-creation.ts"
      provides: "Extended TripCreationState with step: 1|2|3 and selectedTone"
      exports: ["useTripCreationStore"]
      contains: "selectedTone: StoryTone"
    - path: "src/components/upload/StepIndicator.tsx"
      provides: "3-step indicator with aria-label='Step X of 3'"
      contains: "'Step ${step} of 3'"
    - path: "src/app/globals.css"
      provides: ".progress-indeterminate keyframe + class + reduced-motion media query"
      contains: "@keyframes progress-indeterminate"
  key_links:
    - from: "src/stores/trip-creation.ts"
      to: "src/types/database.ts"
      via: "import type { StoryTone } from '@/types/database'"
      pattern: "StoryTone"

threat_model_notes:
  trust_boundary: "Client-only files; no new trust boundary introduced."
---

<objective>
Extend three existing artefacts to support Phase 3 surfaces, without touching any Phase 3 lib code or new components:

1. **Zustand store** — extend `step: 1 | 2` → `1 | 2 | 3`; add `selectedTone: StoryTone` (default `'cinematic'` per D-08); add `setSelectedTone` action. Per D-11, the store must also accept being initialised at step 3 with a pre-loaded `tripId` (the wizard hydrates from URL params in Plan 03-03).
2. **StepIndicator** — accept `step: 1 | 2 | 3`; render three nodes with labels `Details / Photos / Tone`; update aria-label from `Step X of 2` to `Step X of 3`. No visual chrome changes.
3. **globals.css** — add `.progress-indeterminate` keyframe animation + class (per UI-SPEC §Generation Progress Bar) with a `@media (prefers-reduced-motion: reduce)` guard. This is the CSS keyframe the future GenerationProgress component (Plan 03-03) will apply — declaring it now avoids cross-plan file overlap in Wave 2.

Purpose: These three foundational updates are file-disjoint from Plan 03-01 (Gemini lib) and can run in Wave 1 in parallel. They unblock all downstream plans without introducing any new components or routes.

Output: Three modified files; no new files. The wizard continues to work in step 1 and step 2 unchanged (no Step 3 UI yet — that lands in Plan 03-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-story-generation/03-CONTEXT.md
@.planning/phases/03-ai-story-generation/03-UI-SPEC.md
@.planning/phases/03-ai-story-generation/03-RESEARCH.md

<interfaces>
<!-- Current store shape (must be extended, not rewritten). -->

From src/stores/trip-creation.ts (current):
```typescript
interface TripCreationState {
  step: 1 | 2
  tripId: string | null
  tripTitle: string
  tripDestination: string
  setStep: (step: 1 | 2) => void
  setTripId: (id: string) => void
  setTripTitle: (title: string) => void
  setTripDestination: (dest: string) => void
  reset: () => void
}
```

From src/components/upload/StepIndicator.tsx (current):
```typescript
interface StepIndicatorProps { step: 1 | 2 }
const LABELS = ['Details', 'Photos']
aria-label={`Step ${step} of 2`}
```

From src/types/database.ts:
```typescript
export type StoryTone = 'cinematic' | 'poetic' | 'adventurous' | 'documentary'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend Zustand store with step 3 and selectedTone</name>
  <files>src/stores/trip-creation.ts</files>
  <read_first>
    - src/stores/trip-creation.ts (entire current file — preserve all existing fields and actions; only ADD; do not rename)
    - src/types/database.ts (StoryTone type — import it; do not re-declare)
    - .planning/phases/03-ai-story-generation/03-CONTEXT.md D-07, D-08, D-11
    - .planning/phases/03-ai-story-generation/03-RESEARCH.md "Pattern 5: Zustand Store Extension" (exact field shape)
  </read_first>
  <behavior>
    - Test 1: `useTripCreationStore.getState().step` type is `1 | 2 | 3` (TypeScript compiler enforces)
    - Test 2: `useTripCreationStore.getState().selectedTone` returns `'cinematic'` by default
    - Test 3: `useTripCreationStore.getState().setSelectedTone('poetic')` updates `selectedTone` to `'poetic'`
    - Test 4: `useTripCreationStore.getState().setStep(3)` is accepted by the type system; `setStep` accepts `1 | 2 | 3`
    - Test 5: `useTripCreationStore.getState().reset()` returns selectedTone to 'cinematic' and step to 1
    - Test 6: All existing fields (tripId, tripTitle, tripDestination, setTripId, setTripTitle, setTripDestination) continue to work unchanged
  </behavior>
  <action>
    Edit `src/stores/trip-creation.ts`:

    1. Add `import type { StoryTone } from '@/types/database'` at the top.
    2. In the `TripCreationState` interface, change `step: 1 | 2` to `step: 1 | 2 | 3`.
    3. In the same interface, change `setStep: (step: 1 | 2) => void` to `setStep: (step: 1 | 2 | 3) => void`.
    4. Add a new field to the interface: `selectedTone: StoryTone`.
    5. Add a new action to the interface: `setSelectedTone: (tone: StoryTone) => void`.
    6. In `initialState`, add `selectedTone: 'cinematic' as StoryTone,` (D-08 default).
    7. In the `create` body, add `setSelectedTone: (selectedTone) => set({ selectedTone }),` after `setTripDestination`.
    8. The existing `step: 1 as const` literal in initialState may need to be `1 as 1 | 2 | 3` for TypeScript narrowing — adjust as the compiler demands. Do NOT change the runtime default (still step 1).

    Do NOT remove or rename any existing field or action. Do NOT add persist middleware. The reset action automatically picks up the new `selectedTone` field because it spreads `initialState`.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "step: 1 \| 2 \| 3" src/stores/trip-creation.ts` returns ≥ 2 matches (state field type + setStep signature).
    - `grep -E "setSelectedTone" src/stores/trip-creation.ts` returns ≥ 2 matches (interface declaration + create body).
    - `grep -F "selectedTone: 'cinematic'" src/stores/trip-creation.ts` returns 1 match (initialState default).
    - `grep -F "import type { StoryTone } from '@/types/database'" src/stores/trip-creation.ts` returns 1 match.
    - `grep -E "setTripId|setTripTitle|setTripDestination|tripId|tripTitle|tripDestination" src/stores/trip-creation.ts` returns ≥ 6 matches (existing fields preserved).
    - `grep -E "^export const useTripCreationStore" src/stores/trip-creation.ts` returns 1 match (export preserved).
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>The store accepts step 3 and exposes `selectedTone` + `setSelectedTone`; all Phase 2 fields and actions still exist; project compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend StepIndicator to 3 steps with 'Tone' label</name>
  <files>src/components/upload/StepIndicator.tsx</files>
  <read_first>
    - src/components/upload/StepIndicator.tsx (entire current file — extend only)
    - .planning/phases/03-ai-story-generation/03-UI-SPEC.md §"StepIndicator Update"
    - .planning/phases/03-ai-story-generation/03-RESEARCH.md "Pattern 8: StepIndicator Update"
  </read_first>
  <behavior>
    - Test 1: `<StepIndicator step={3} />` renders without TypeScript error
    - Test 2: When step={1}, aria-label is "Step 1 of 3"; when step={3}, aria-label is "Step 3 of 3"
    - Test 3: The component renders three steps: Details / Photos / Tone (in that order)
    - Test 4: When step={3}, the third node shows the active amber circle + glow (same active styling as Phase 2 active state) and the first two are 'done' (amber check)
    - Test 5: All existing motion behavior (segment bar scaleX entrance, useReducedMotion gate) continues to work
  </behavior>
  <action>
    Edit `src/components/upload/StepIndicator.tsx`:

    1. Change the prop type: `interface StepIndicatorProps { step: 1 | 2 }` → `interface StepIndicatorProps { step: 1 | 2 | 3 }`.
    2. Extend the labels array: `const LABELS = ['Details', 'Photos']` → `const LABELS = ['Details', 'Photos', 'Tone']`.
    3. Inside the map callback, change `const n = (i + 1) as 1 | 2` to `const n = (i + 1) as 1 | 2 | 3`.
    4. Update the aria-label: `aria-label={`Step ${step} of 2`}` → `aria-label={`Step ${step} of 3`}`.

    No other changes. The existing active/done/idle styling, the segment bar animation, useReducedMotion gating, and the step-bar-glow class all continue to apply automatically to the new third node.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "step: 1 | 2 | 3" src/components/upload/StepIndicator.tsx` returns 1 match.
    - `grep -F "['Details', 'Photos', 'Tone']" src/components/upload/StepIndicator.tsx` returns 1 match.
    - `grep -F "Step ${step} of 3" src/components/upload/StepIndicator.tsx` returns 1 match.
    - `grep -F "(i + 1) as 1 | 2 | 3" src/components/upload/StepIndicator.tsx` returns 1 match.
    - `grep -F "Step ${step} of 2" src/components/upload/StepIndicator.tsx` returns 0 matches (old aria-label removed).
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>StepIndicator renders 3 steps; aria-label says "Step X of 3"; type union extended; no visual regressions to Phase 2 step 1/2 rendering.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add progress-indeterminate CSS keyframe + reduced motion guard to globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (entire current file — append at end; preserve all existing rules including the reduced-motion media query for .card-enter)
    - .planning/phases/03-ai-story-generation/03-UI-SPEC.md §"Generation Progress Bar" and §"Tailwind Config Contract" (the CSS to add — copy verbatim)
  </read_first>
  <behavior>
    - Test 1: globals.css contains `@keyframes progress-indeterminate` with `0% { transform: translateX(-100%); }` and `100% { transform: translateX(400%); }`
    - Test 2: globals.css contains `.progress-indeterminate { animation: progress-indeterminate 1.8s ease-in-out infinite; }`
    - Test 3: globals.css contains a `@media (prefers-reduced-motion: reduce)` rule that disables the animation on `.progress-indeterminate` and sets `width: 100%; opacity: 0.4;`
    - Test 4: All existing globals.css rules (gradient-fade-bottom, ambient-glow, surface-card, shimmer, grain-overlay, float-bokeh, expand-ring, glow-breathe, card-enter, nav-accent-line, step-bar-glow) remain intact
  </behavior>
  <action>
    Append the following block to the end of `src/app/globals.css` (after the existing `.step-bar-glow` rule). Do not modify any existing rule:

    ```
    /* ── Generation progress bar (Phase 3 — Wizard Step 3 + regeneration on /trip/[id]) ─── */
    @keyframes progress-indeterminate {
      0%   { transform: translateX(-100%); }
      100% { transform: translateX(400%); }
    }

    .progress-indeterminate {
      animation: progress-indeterminate 1.8s ease-in-out infinite;
    }

    @media (prefers-reduced-motion: reduce) {
      .progress-indeterminate {
        animation: none;
        width: 100%;
        opacity: 0.4;
      }
    }
    ```

    Verify by visual inspection that no existing rule was removed or reordered.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "@keyframes progress-indeterminate" src/app/globals.css` returns 1 match.
    - `grep -F "transform: translateX(-100%);" src/app/globals.css` returns 1 match.
    - `grep -F "transform: translateX(400%);" src/app/globals.css` returns 1 match.
    - `grep -F "animation: progress-indeterminate 1.8s ease-in-out infinite;" src/app/globals.css` returns 1 match.
    - `grep -c "prefers-reduced-motion: reduce" src/app/globals.css` returns ≥ 2 (the new block + the existing card-enter block).
    - `grep -F "step-bar-glow" src/app/globals.css` returns ≥ 1 match (existing rule preserved).
    - `grep -F "shimmer" src/app/globals.css` returns ≥ 2 matches (existing rule preserved).
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>globals.css has the new progress-indeterminate keyframe + class + reduced-motion guard appended; no existing rule modified; project builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only extends client-side state types, label arrays, and CSS — no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | src/stores/trip-creation.ts | accept | Zustand store is in-memory client state; no security boundary; tone tampering can only affect that client's prompt — server validates on POST in Plan 03-03 |
</threat_model>

<verification>
**Build verification:** `pnpm build` exits 0 after all 3 tasks.

**No-regression checks:**
- `grep -F "step === 1" src/components/upload/TripWizard.tsx` returns ≥ 1 match (Step 1 branch still intact — TripWizard not modified in this plan).
- `pnpm dev` starts; visiting `/new` still shows Step 1 with the indicator displaying "1 Details ... 2 Photos ... 3 Tone" (third node now visible but idle/dim).
</verification>

<success_criteria>
- Store extension done: step union widened, selectedTone field with default 'cinematic', setSelectedTone action.
- StepIndicator accepts step={3}, labels include 'Tone', aria-label says "Step X of 3".
- globals.css has the progress-indeterminate keyframe + class + reduced-motion guard.
- `pnpm build` exits 0.
- No existing functionality (Step 1, Step 2, photo upload, trip dashboard) is affected.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ai-story-generation/03-02-SUMMARY.md` with:
- Files modified (3)
- Confirmation that step union is now `1 | 2 | 3`
- Confirmation that selectedTone defaults to 'cinematic' (D-08)
- Confirmation that the progress-indeterminate CSS is in place with reduced-motion guard
- Any deviations from the plan
- Next: Plan 03-03 (API route + Wizard Step 3 + ToneSelector) consumes these foundation changes in Wave 2.
</output>
