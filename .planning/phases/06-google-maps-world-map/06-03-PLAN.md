---
phase: 06-google-maps-world-map
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - src/components/viewer/CinematicViewer.tsx
autonomous: true
requirements:
  - PHASE6-VIEWER-API-PROVIDER
must_haves:
  truths:
    - "CinematicViewer.tsx wraps the ViewerMap usage site with APIProvider from @vis.gl/react-google-maps"
    - "APIProvider receives apiKey={process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY!}"
    - "The dynamic import comment is updated to reflect @vis.gl/react-google-maps instead of mapbox-gl"
    - "All existing CinematicViewer props, state, and JSX are otherwise unchanged"
    - "APIProvider is inside the CinematicViewer client component — NOT in any RSC or global layout"
    - "The dynamic import pattern (ssr: false) is preserved"
  artifacts:
    - path: "src/components/viewer/CinematicViewer.tsx"
      provides: "APIProvider context for ViewerMap Google Maps instance"
      contains: "APIProvider"
  key_links:
    - from: "src/components/viewer/CinematicViewer.tsx"
      to: "src/components/viewer/ViewerMap.tsx"
      via: "APIProvider wraps ViewerMap; useMap() in ViewerMap resolves via APIProvider context"
      pattern: "APIProvider"
---

<objective>
Add APIProvider from @vis.gl/react-google-maps to CinematicViewer.tsx, wrapping the ViewerMap render site. This is a small, surgical change — one new import, APIProvider wrapping ~7 lines of JSX. All other CinematicViewer code is untouched.

Purpose: useMap() inside ViewerMap.tsx requires being a descendant of APIProvider. Without this wrapper, the Google Maps map instance will not be available and map.panTo/setZoom will fail silently.
Output: CinematicViewer.tsx with APIProvider wrapping ViewerMap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-google-maps-world-map/06-CONTEXT.md
@.planning/phases/06-google-maps-world-map/06-UI-SPEC.md
</context>

<interfaces>
<!-- Current CinematicViewer.tsx structure — read the full file before editing -->

From src/components/viewer/CinematicViewer.tsx:

Dynamic import (lines 14–18):
```typescript
// Dynamic import — mapbox-gl uses browser APIs; cannot be server-rendered
const ViewerMap = dynamic(
  () => import('@/components/viewer/ViewerMap'),
  { ssr: false }
)
```
Update the comment to: "// Dynamic import — @vis.gl/react-google-maps uses browser APIs; cannot be server-rendered"

ViewerMap render site (lines 115–121):
```tsx
<ViewerMap
  chapterCoords={coords}
  activeChapterIndex={activeChapterIndex}
  pinPlacingForChapter={pinPlacingForChapter}
  onPinPlaced={handlePinPlaced}
  onCancelPinPlacing={() => setPinPlacingForChapter(undefined)}
/>
```
This block must be wrapped in:
```tsx
<APIProvider apiKey={process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY!}>
  <ViewerMap ... />
</APIProvider>
```

All other props, state, handlers, and JSX in CinematicViewer.tsx remain unchanged.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add APIProvider import and wrap ViewerMap in CinematicViewer.tsx</name>
  <files>src/components/viewer/CinematicViewer.tsx</files>
  <read_first>
    - src/components/viewer/CinematicViewer.tsx — read the ENTIRE file before editing; identify: (1) the import block at the top, (2) the dynamic import comment (line 14), (3) the ViewerMap render site (lines 115–121), (4) the LocationEditor section immediately after ViewerMap (lines 123–141)
    - .planning/phases/06-google-maps-world-map/06-UI-SPEC.md §ViewerMap spec — "APIProvider placement" block — confirms APIProvider goes in CinematicViewer.tsx, wraps ViewerMap, uses NEXT_PUBLIC_GOOGLE_MAPS_API_KEY, must NOT be in global layout or RSC
  </read_first>
  <action>
    Make exactly three changes to CinematicViewer.tsx:

    Change 1 — Add APIProvider import:
    In the existing import block, add this line after the line that imports from 'next/dynamic':
      import { APIProvider } from '@vis.gl/react-google-maps'

    Change 2 — Update dynamic import comment:
    Find the comment: "// Dynamic import — mapbox-gl uses browser APIs; cannot be server-rendered"
    Replace with: "// Dynamic import — @vis.gl/react-google-maps uses browser APIs; cannot be server-rendered"

    Change 3 — Wrap ViewerMap with APIProvider:
    Find the <ViewerMap ... /> block (currently lines ~115–121, 6 lines with props spread across them).
    Wrap it so it becomes:
      <APIProvider apiKey={process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY!}>
        <ViewerMap
          chapterCoords={coords}
          activeChapterIndex={activeChapterIndex}
          pinPlacingForChapter={pinPlacingForChapter}
          onPinPlaced={handlePinPlaced}
          onCancelPinPlacing={() => setPinPlacingForChapter(undefined)}
        />
      </APIProvider>

    Preserve the surrounding JSX exactly — the comment block before ViewerMap ("Story content — offset from fixed map panel...") and the LocationEditor section immediately after must remain unchanged.

    Do NOT modify: component props, state variables, handler functions, LocationEditor JSX, ViewerNav, ScrollProgress, ChapterSection, AllPhotosSection, or any other JSX.

    Do NOT add APIProvider to any layout file (src/app/layout.tsx) or any RSC. It must stay inside the CinematicViewer 'use client' component.
  </action>
  <verify>
    <automated>
      node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/viewer/CinematicViewer.tsx','utf8'); const hasImport=s.includes(\"from '@vis.gl/react-google-maps'\"); const hasProvider=s.includes('APIProvider'); const hasApiKey=s.includes('NEXT_PUBLIC_GOOGLE_MAPS_API_KEY'); const noMapbox=!s.includes('mapbox-gl'); process.exit((hasImport&&hasProvider&&hasApiKey&&noMapbox) ? 0 : 1)"
    </automated>
  </verify>
  <acceptance_criteria>
    - CinematicViewer.tsx contains "from '@vis.gl/react-google-maps'" import
    - CinematicViewer.tsx contains "APIProvider"
    - CinematicViewer.tsx contains "NEXT_PUBLIC_GOOGLE_MAPS_API_KEY"
    - CinematicViewer.tsx does NOT contain "mapbox-gl" (comment update confirmed)
    - CinematicViewer.tsx still contains the dynamic import with { ssr: false }
    - CinematicViewer.tsx still contains all original props: chapterCoords, activeChapterIndex, pinPlacingForChapter, onPinPlaced, onCancelPinPlacing on the ViewerMap render
    - CinematicViewer.tsx still contains LocationEditor JSX unchanged
    - The APIProvider wraps only the ViewerMap, not the entire return statement
  </acceptance_criteria>
  <done>CinematicViewer.tsx has APIProvider wrapping ViewerMap. The Google Maps context is now available to ViewerMap's useMap() hook. All other CinematicViewer code is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process.env → APIProvider apiKey | NEXT_PUBLIC_GOOGLE_MAPS_API_KEY passed to APIProvider; exposed to client bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Information Disclosure | APIProvider apiKey in client bundle | mitigate | Key restricted to HTTP referrers (D-13 — human action in Plan 06-06); acceptable Maps JS API exposure pattern |
| T-06-07 | Spoofing | APIProvider wrapping scope | accept | APIProvider is placed only in CinematicViewer (authenticated route); no global layout placement means it only loads on viewer routes — minimal attack surface |
</threat_model>

<verification>
After CinematicViewer.tsx is updated:

1. Confirm no TypeScript errors:
   pnpm exec tsc --noEmit --skipLibCheck

2. Confirm APIProvider is scoped to the viewer, not global layout:
   grep -r "APIProvider" src/app/layout.tsx — should return 0 matches

3. Confirm all existing ViewerMap props are still passed:
   grep -A8 "APIProvider" src/components/viewer/CinematicViewer.tsx — should show chapterCoords, activeChapterIndex, pinPlacingForChapter, onPinPlaced, onCancelPinPlacing
</verification>

<success_criteria>
- CinematicViewer.tsx imports and renders APIProvider from @vis.gl/react-google-maps
- APIProvider wraps exactly the ViewerMap component
- APIProvider apiKey references process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY!
- No other changes to CinematicViewer.tsx
- pnpm exec tsc --noEmit --skipLibCheck exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/06-google-maps-world-map/06-03-SUMMARY.md`
</output>
