---
phase: 06-google-maps-world-map
plan: 05
type: execute
wave: 2
depends_on:
  - "06-04"
files_modified:
  - src/app/(marketing)/page.tsx
autonomous: true
requirements:
  - PHASE6-LANDING-PAGE
must_haves:
  truths:
    - "page.tsx does not contain journeyPins, WORLD_PATHS, JourneyPin, or the old WorldMapSection function"
    - "page.tsx imports WorldMap from '@/components/ui/world-map'"
    - "A new WorldMapSection function exists in page.tsx that renders the WorldMap component"
    - "JOURNEY_ARCS constant exists in page.tsx with exactly 8 arc pairs using the lat/lng from the 12 source locations"
    - "The section headline 'Journeys archived across the world.' uses letter-by-letter motion.span animation gated on useInView and useReducedMotion"
    - "The WorldMapSection uses RevealDiv pattern (reveal-up CSS class via useReveal hook) for the header block"
    - "Side description copy reads 'Every arc is a real journey. Twelve trips. One archive.'"
    - "Eyebrow text reads 'The archive' — font-sans text-[10px] uppercase tracking-[0.26em] text-parchment-600"
    - "The WorldMap is rendered inside a rounded-[2rem] border border-white/[0.06] bg-white/[0.015] container div"
    - "Ambient glow radial gradient is preserved in the section"
    - "The rest of page.tsx (MarketingPage, HeroSection, ZigzagSection, StatsSection, etc.) is unchanged"
  artifacts:
    - path: "src/app/(marketing)/page.tsx"
      provides: "Landing page with new WorldMapSection using D3 arc world map"
      contains: "WorldMap"
    - path: "src/app/(marketing)/page.tsx"
      provides: "JOURNEY_ARCS constant with 8 arc pairs"
      contains: "JOURNEY_ARCS"
  key_links:
    - from: "src/app/(marketing)/page.tsx WorldMapSection"
      to: "src/components/ui/world-map.tsx"
      via: "import WorldMap; <WorldMap dots={JOURNEY_ARCS} />"
      pattern: "WorldMap"
    - from: "JOURNEY_ARCS constant"
      to: "WorldMap dots prop"
      via: "passed directly as dots={JOURNEY_ARCS}"
      pattern: "JOURNEY_ARCS"
---

<objective>
Replace the old WorldMapSection (SVG pins, JourneyPin tooltips, WORLD_PATHS) in src/app/(marketing)/page.tsx with a new WorldMapSection that wraps the WorldMap component from Plan 04. Delete journeyPins, WORLD_PATHS, and JourneyPin. Add JOURNEY_ARCS. Wire up letter-by-letter headline animation.

Purpose: The landing page world map section moves from static hover-pin UX to the animated arc world map that visually communicates global journey reach.
Output: page.tsx with WorldMapSection replacement complete; all deleted code removed; WorldMap component integrated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-google-maps-world-map/06-CONTEXT.md
@.planning/phases/06-google-maps-world-map/06-UI-SPEC.md
</context>

<interfaces>
<!-- Key structures from 06-UI-SPEC.md §WorldMapSection replacement and §JOURNEY_ARCS -->

JOURNEY_ARCS constant (from 06-UI-SPEC.md — use these exact coordinates):
```typescript
const JOURNEY_ARCS = [
  { start: { lat: 65, lng: -18 },  end: { lat: -3,  lng: 37  } },  // Iceland → Kilimanjaro
  { start: { lat: 63, lng: -153 }, end: { lat: -51, lng: -72 } },  // Alaska → Patagonia
  { start: { lat: 32, lng: -5  },  end: { lat: 9,   lng: 38  } },  // Morocco → Ethiopia
  { start: { lat: 65, lng: -18 },  end: { lat: 47,  lng: 103 } },  // Iceland → Mongolia
  { start: { lat: 36, lng: 138 },  end: { lat: -41, lng: 172 } },  // Japan → New Zealand
  { start: { lat: 4,  lng: -74 },  end: { lat: 32,  lng: -5  } },  // Colombia → Morocco
  { start: { lat: 53, lng: 108 },  end: { lat: 1,   lng: 114 } },  // Siberia → Borneo
  { start: { lat: 63, lng: -153 }, end: { lat: 36,  lng: 138 } },  // Alaska → Japan
]
```

New WorldMapSection structure (from 06-UI-SPEC.md §WorldMapSection replacement):
- <section className="relative py-24 md:py-36 overflow-hidden">
- Ambient glow div (carry forward from old WorldMapSection — radial-gradient ellipse 90% 60%)
- <div className="mx-auto max-w-7xl px-8 md:px-16">
- Header block uses useReveal() hook → ref passed to div with className "reveal-up mb-16"
- Header internal structure: eyebrow row (h-px w-6 amber hairline + "The archive" text) + grid [1fr auto] for h2 + side paragraph
- h2 contains letter-by-letter motion.span animation on "Journeys archived across the world."
- Side paragraph: "Every arc is a real journey. Twelve trips. One archive." — font-sans text-[13px] leading-[1.7] text-parchment-400 max-w-[260px] md:mt-0 md:text-right
- WorldMap container: <div className="relative overflow-hidden rounded-[2rem] border border-white/[0.06] bg-white/[0.015]">
- <WorldMap dots={JOURNEY_ARCS} /> inside the container div

Letter-by-letter headline animation spec (from 06-UI-SPEC.md §Motion Contract):
- Split "Journeys archived across the world." into individual characters
- useInView({ once: true, margin: '-80px' }) on the section (or heading ref)
- Each character: <motion.span> with initial={{ opacity:0, y:10 }}, animate={isInView ? {opacity:1,y:0} : {opacity:0,y:10}}, transition={{ duration:0.5, delay:charIndex*0.03, ease:[0.22,1,0.36,1] }}
- Spaces: render as <motion.span style={{ display:'inline-block', width:'0.3em' }}> (non-breaking space)
- When shouldReduceMotion: render the headline as a plain text string inside the h2, no per-character wrapping

useReveal hook: already exists in page.tsx (lines 8–26). Reuse it — do not redeclare.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete old WorldMapSection code and add JOURNEY_ARCS constant</name>
  <files>src/app/(marketing)/page.tsx</files>
  <read_first>
    - src/app/(marketing)/page.tsx — read the ENTIRE file before editing; identify exact line numbers for: (1) journeyPins constant (lines 79–104), (2) WORLD_PATHS constant (lines 668–753), (3) WorldMapSection function (lines 755–891), (4) JourneyPin function (lines 893–976)
    - .planning/phases/06-google-maps-world-map/06-CONTEXT.md §D-09 — confirms deletion of all four items
    - .planning/phases/06-google-maps-world-map/06-UI-SPEC.md §JOURNEY_ARCS — exact 8 arc pair coordinates to add
  </read_first>
  <action>
    This task makes three changes to page.tsx:

    Change 1 — Delete journeyPins constant:
    Remove the entire journeyPins constant (lines 79–104) including the comment above it.
    This is the array: const journeyPins = [ { id: 1, x: 45.0, ... }, ... ]

    Change 2 — Add JOURNEY_ARCS constant and WorldMap import:
    After the existing imports block (after the last import statement at the top of the file), add:
      import { WorldMap } from '@/components/ui/world-map'

    In the data section (near where journeyPins was — after the stats constant), add:
      const JOURNEY_ARCS = [
        { start: { lat: 65, lng: -18 },  end: { lat: -3,  lng: 37  } },
        { start: { lat: 63, lng: -153 }, end: { lat: -51, lng: -72 } },
        { start: { lat: 32, lng: -5  },  end: { lat: 9,   lng: 38  } },
        { start: { lat: 65, lng: -18 },  end: { lat: 47,  lng: 103 } },
        { start: { lat: 36, lng: 138 },  end: { lat: -41, lng: 172 } },
        { start: { lat: 4,  lng: -74 },  end: { lat: 32,  lng: -5  } },
        { start: { lat: 53, lng: 108 },  end: { lat: 1,   lng: 114 } },
        { start: { lat: 63, lng: -153 }, end: { lat: 36,  lng: 138 } },
      ]

    Change 3 — Delete WORLD_PATHS constant:
    Remove the entire WORLD_PATHS constant (lines 668–753) including its comment header.
    This is: const WORLD_PATHS = [ "M87,72 ...", ... ]

    Do NOT yet touch the WorldMapSection function or JourneyPin function — those are replaced in Task 2.
    Do NOT modify any other code in the file.
  </action>
  <verify>
    <automated>
      node -e "const fs=require('fs'); const s=fs.readFileSync('src/app/(marketing)/page.tsx','utf8'); const ok=!s.includes('journeyPins') && !s.includes('WORLD_PATHS') && s.includes('JOURNEY_ARCS') && s.includes(\"from '@/components/ui/world-map'\"); process.exit(ok ? 0 : 1)"
    </automated>
  </verify>
  <acceptance_criteria>
    - page.tsx does NOT contain "journeyPins" anywhere (neither the const declaration nor array usage)
    - page.tsx does NOT contain "WORLD_PATHS" anywhere
    - page.tsx contains "JOURNEY_ARCS" constant with 8 arc objects
    - page.tsx contains "from '@/components/ui/world-map'" import
    - The JOURNEY_ARCS constant contains exactly 8 entries (count { start: patterns)
    - All other sections (MarketingPage, zigzagFeatures, stats, marqueeItems, useReveal, useParallax) are unchanged
  </acceptance_criteria>
  <done>journeyPins and WORLD_PATHS deleted; JOURNEY_ARCS added with 8 arc pairs; WorldMap imported.</done>
</task>

<task type="auto">
  <name>Task 2: Replace WorldMapSection function and delete JourneyPin component</name>
  <files>src/app/(marketing)/page.tsx</files>
  <read_first>
    - src/app/(marketing)/page.tsx — after Task 1 completes; read the current state of the file to find the WorldMapSection function and JourneyPin function; confirm line ranges for replacement
    - .planning/phases/06-google-maps-world-map/06-UI-SPEC.md §WorldMapSection replacement — full JSX layout spec; ambient glow; RevealDiv header; letter-by-letter headline animation spec; side description copy
    - .planning/phases/06-google-maps-world-map/06-UI-SPEC.md §Motion Contract "WorldMapSection headline letter-by-letter" — exact motion.span animation spec with charIndex * 0.03 delay
    - src/app/(marketing)/page.tsx — confirm useReveal hook is already defined (lines 8–26); confirm useInView is imported from framer-motion at the top; if useInView is not currently imported, add it to the framer-motion import
  </read_first>
  <action>
    This task makes two changes to page.tsx:

    Change 1 — Replace WorldMapSection function:
    Find the entire WorldMapSection function (from "// ─── WorldMapSection ───" to the closing brace at line ~891).
    Delete it entirely.
    Replace with a new WorldMapSection function that:

    a) Uses the existing useReveal() hook for the heading container (ref → 'reveal-up mb-16' div)
    b) Uses useInView from framer-motion for the letter-by-letter headline animation trigger:
       const headlineRef = useRef<HTMLHeadingElement>(null)
       const isHeadlineInView = useInView(headlineRef, { once: true, margin: '-80px' })
    c) Uses shouldReduceMotion from useReducedMotion() — get this from the outer MarketingPage
       function's shouldReduce variable via prop or re-declare locally:
       const shouldReduceMotion = useReducedMotion()
    d) Renders the letter-by-letter headline:
       Split the string "Journeys archived across the world." into characters.
       For each character: if it is a space, render a <motion.span style={{ display:'inline-block', width:'0.3em' }} key={i} />
       For non-space characters, render:
         <motion.span
           key={i}
           style={{ display: 'inline-block' }}
           initial={{ opacity: 0, y: 10 }}
           animate={isHeadlineInView ? { opacity: 1, y: 0 } : { opacity: 0, y: 10 }}
           transition={{ duration: 0.5, delay: i * 0.03, ease: [0.22, 1, 0.36, 1] }}
         >
           {char}
         </motion.span>
       When shouldReduceMotion: render the headline text as a plain string inside the h2, no motion.span wrapping.

    e) Full JSX structure of the new WorldMapSection:
       <section className="relative py-24 md:py-36 overflow-hidden">
         <div aria-hidden className="pointer-events-none absolute inset-0" style={{ background: 'radial-gradient(ellipse 90% 60% at 50% 50%, rgba(229,166,99,0.04) 0%, transparent 65%)' }} />
         <div className="mx-auto max-w-7xl px-8 md:px-16">
           <div ref={headingRef} className="reveal-up mb-16">
             {/* headingRef is from useReveal() */}
             <div className="flex items-center gap-3 mb-5">
               <div className="h-px w-6 bg-amber-accent/50" />
               <p className="font-sans text-[10px] uppercase tracking-[0.26em] text-parchment-600">The archive</p>
             </div>
             <div className="grid md:grid-cols-[1fr_auto] md:items-end md:gap-16">
               <h2 ref={headlineRef} className="font-serif font-medium uppercase leading-[0.94] text-ink-50" style={{ fontSize: 'clamp(28px, 4.5vw, 52px)' }}>
                 {/* Letter-by-letter animation OR plain text when reduced motion */}
               </h2>
               <p className="mt-4 max-w-[260px] font-sans text-[13px] leading-[1.7] text-parchment-400 md:mt-0 md:text-right">
                 Every arc is a real journey. Twelve trips. One archive.
               </p>
             </div>
           </div>
           <div className="relative overflow-hidden rounded-[2rem] border border-white/[0.06] bg-white/[0.015]">
             <WorldMap dots={JOURNEY_ARCS} />
           </div>
         </div>
       </section>

       Note on ref usage: useReveal() returns a ref for the heading container div (the 'reveal-up' div).
       The headlineRef (for useInView) goes on the h2 element. These are two different refs.
       Name them: headingRef = useReveal() and headlineRef = useRef<HTMLHeadingElement>(null).

    Change 2 — Delete JourneyPin function:
    Find the entire JourneyPin function (from "// ─── JourneyPin ───" to the end of the file at line ~976).
    Delete it entirely.

    After both changes, confirm page.tsx still ends with a valid closing brace. The file should end
    after the WorldMapSection closing brace — there should be no dangling closing braces or missing code.

    Confirm MarketingPage still calls <WorldMapSection /> in the same location in its JSX where
    the old WorldMapSection was rendered. No changes needed there — the function name is the same.
  </action>
  <verify>
    <automated>
      node -e "const fs=require('fs'); const s=fs.readFileSync('src/app/(marketing)/page.tsx','utf8'); const noOld=!s.includes('JourneyPin') && !s.includes('WORLD_PATHS') && !s.includes('journeyPins') && !s.includes('setActivePin') && !s.includes('setVisiblePins'); const hasNew=s.includes('WorldMap') && s.includes('JOURNEY_ARCS') && s.includes('Every arc is a real journey'); process.exit((noOld&&hasNew) ? 0 : 1)"
    </automated>
  </verify>
  <acceptance_criteria>
    - page.tsx does NOT contain "JourneyPin" (component name, neither function nor JSX reference)
    - page.tsx does NOT contain "setActivePin" or "setVisiblePins" (old WorldMapSection state)
    - page.tsx does NOT contain "visiblePins" or "activePin" state variables
    - page.tsx does NOT contain any reference to the old bottom info strip ("Archived journey", "Active story", "hover to explore")
    - page.tsx contains "WorldMap" component usage inside WorldMapSection
    - page.tsx contains "JOURNEY_ARCS" passed as dots prop: dots={JOURNEY_ARCS}
    - page.tsx contains "Every arc is a real journey. Twelve trips. One archive." (new side description)
    - page.tsx contains "motion.span" for per-character headline animation
    - page.tsx contains "The archive" eyebrow text with correct font-sans text-[10px] uppercase tracking-[0.26em] classes
    - page.tsx WorldMapSection contains ambient glow radial gradient div
    - page.tsx contains "rounded-[2rem] border border-white/[0.06] bg-white/[0.015]" WorldMap container
    - page.tsx is valid — no syntax errors (pnpm run build or tsc check)
    - All sections other than WorldMapSection (hero, zigzag, stats, CTA, marquee) are unchanged
  </acceptance_criteria>
  <done>WorldMapSection replaced with new arc-animation version. JourneyPin deleted. page.tsx is clean with no legacy WorldMap code remaining.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JOURNEY_ARCS → WorldMap SVG | Static constant — no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | JOURNEY_ARCS constant values | accept | Hardcoded constant in page.tsx; not user-supplied; no sanitization needed |
| T-06-11 | Information Disclosure | Marketing page is public | accept | Marketing page is intentionally public; no authenticated data exposed |
</threat_model>

<verification>
After both tasks complete:

1. TypeScript check:
   pnpm exec tsc --noEmit --skipLibCheck

2. Grep for deleted symbols — all must return 0 matches:
   grep -c "journeyPins" src/app/(marketing)/page.tsx
   grep -c "WORLD_PATHS" src/app/(marketing)/page.tsx
   grep -c "JourneyPin" src/app/(marketing)/page.tsx

3. Build check (full):
   pnpm run build — should exit 0

4. Confirm WorldMapSection is still called in MarketingPage:
   grep "WorldMapSection" src/app/(marketing)/page.tsx — should return 2 matches (function definition + JSX usage)
</verification>

<success_criteria>
- page.tsx has zero references to journeyPins, WORLD_PATHS, JourneyPin
- JOURNEY_ARCS constant has 8 arc pairs
- New WorldMapSection renders WorldMap with JOURNEY_ARCS dots
- Letter-by-letter headline animation implemented with useReducedMotion gate
- pnpm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/06-google-maps-world-map/06-05-SUMMARY.md`
</output>
