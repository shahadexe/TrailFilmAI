---
phase: 06-google-maps-world-map
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/components/viewer/ViewerMap.tsx
autonomous: true
requirements:
  - PHASE6-VIEWER-MAP
must_haves:
  truths:
    - "ViewerMap.tsx imports nothing from mapbox-gl"
    - "ViewerMap.tsx imports APIProvider, Map, AdvancedMarker, Polyline, useMap from @vis.gl/react-google-maps"
    - "The ChapterCoord interface and ViewerMapProps interface are identical to the Phase 4 version — zero prop changes"
    - "Map renders with colorScheme='DARK', disableDefaultUI={true}, gestureHandling='greedy', clickableIcons={false}"
    - "Dashed amber Polyline uses SVG icon pattern: path 'M 0,-1 0,1', repeat '12px', strokeOpacity 0 on Polyline itself"
    - "Each chapterCoord renders an AdvancedMarker with an 8px amber dot child div"
    - "Active chapter marker applies marker-pulse CSS animation class (reduced-motion aware)"
    - "flyTo equivalent: useMap() hook + map.panTo + map.setZoom(10) on activeChapterIndex change"
    - "Click-to-place: Map onClick uses e.detail.latLng.lat/lng; pending pin is 14px AdvancedMarker with white border and amber glow"
    - "Responsive layout Tailwind class string is character-for-character identical to the Phase 4 ViewerMap outer div"
    - "Pin-placing overlay JSX (hint text, Cancel button) is verbatim carry-forward from Phase 4"
    - "sr-only aria-live announcement region and role='region' aria-label are preserved"
    - "NoGpsState renders when chapterCoords.length === 0 and !isPlacing"
  artifacts:
    - path: "src/components/viewer/ViewerMap.tsx"
      provides: "Full Google Maps replacement of the Mapbox viewer map"
      contains: "useMap"
    - path: "src/components/viewer/ViewerMap.tsx"
      provides: "Preserved prop interface"
      contains: "ChapterCoord"
  key_links:
    - from: "src/components/viewer/ViewerMap.tsx"
      to: "@vis.gl/react-google-maps"
      via: "Map, AdvancedMarker, Polyline, useMap imports"
      pattern: "from '@vis.gl/react-google-maps'"
    - from: "src/components/viewer/ViewerMap.tsx"
      to: "src/app/globals.css"
      via: "marker-pulse className on active AdvancedMarker child div"
      pattern: "marker-pulse"
    - from: "src/components/viewer/ViewerMap.tsx"
      to: "src/components/viewer/CinematicViewer.tsx"
      via: "exported ChapterCoord and ViewerMap — consumed by CinematicViewer unchanged"
      pattern: "export (function|interface) (ViewerMap|ChapterCoord)"
---

<objective>
Fully replace src/components/viewer/ViewerMap.tsx — rewrite from Mapbox GL JS to @vis.gl/react-google-maps. The prop interface, responsive layout, all behaviors (flyTo→panTo+setZoom, click-to-place, dashed amber Polyline, AdvancedMarker dots, NoGpsState fallback, pin-placing overlay), and accessibility contracts are all preserved identically.

Purpose: CinematicViewer.tsx continues passing the same props without any changes. The viewer experience for users is visually identical — same dark map, same amber path, same amber dots, same layout.
Output: ViewerMap.tsx rewritten; mapbox-gl import gone; @vis.gl/react-google-maps used throughout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-google-maps-world-map/06-CONTEXT.md
@.planning/phases/06-google-maps-world-map/06-UI-SPEC.md
@src/components/viewer/NoGpsState.tsx
</context>

<interfaces>
<!-- Exact interfaces from the existing ViewerMap.tsx that must be preserved verbatim -->

From src/components/viewer/ViewerMap.tsx (current Mapbox implementation — READ BEFORE REWRITING):

```typescript
export interface ChapterCoord {
  chapterIndex: number
  lat: number
  lng: number
  label?: string
}

interface ViewerMapProps {
  chapterCoords: ChapterCoord[]
  activeChapterIndex?: number
  pinPlacingForChapter?: number
  onPinPlaced?: (chapterIndex: number, lat: number, lng: number) => void
  onCancelPinPlacing?: () => void
}
```

Outer container className (must be character-for-character identical):
```
[
  'fixed bottom-0 left-0 right-0 h-[40dvh] z-20',
  'border-t border-[rgba(255,255,255,0.06)] bg-ink/90 backdrop-blur-xl',
  'md:top-0 md:left-auto md:right-0 md:bottom-auto',
  'md:h-[100dvh] md:w-[38vw]',
  'md:border-t-0 md:border-l md:border-[rgba(255,255,255,0.06)]',
  'md:bg-ink md:backdrop-blur-none',
].join(' ')
```

Pin-placing overlay JSX (carry forward verbatim from ViewerMap.tsx lines 211–223):
- Outer div: absolute top-3 inset-x-3 z-10 flex items-center justify-between gap-2 rounded-lg bg-ink/90 backdrop-blur-sm px-3 py-2 border border-amber-accent/30
- Hint text: "Click map to place location pin" — font-sans text-[12px] text-amber-accent
- Cancel button: type="button", "Cancel" — font-sans text-[11px] uppercase tracking-[0.08em] text-parchment-400 hover:text-parchment-200

sr-only announcement span (carry forward verbatim from ViewerMap.tsx lines 227–229):
- className="sr-only" aria-live="polite" aria-atomic="true"
- content: {announcement} state string
- setAnnouncement called with "Map moved to {label}" in the flyTo effect
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite ViewerMap.tsx with @vis.gl/react-google-maps</name>
  <files>src/components/viewer/ViewerMap.tsx</files>
  <read_first>
    - src/components/viewer/ViewerMap.tsx — read the ENTIRE current file before touching it; extract: prop interfaces (ChapterCoord, ViewerMapProps), responsive class string, pin-placing overlay JSX (lines 211–223), sr-only announcement (lines 227–229), NoGpsState usage (line 208)
    - src/components/viewer/NoGpsState.tsx — confirm its import path and that it needs no props
    - .planning/phases/06-google-maps-world-map/06-UI-SPEC.md §ViewerMap — read the FULL ViewerMap spec section including: Map init block, Polyline spec, AdvancedMarker spec, flyTo equivalent, click-to-place spec, responsive layout block, pin-placing overlay, accessibility
    - .planning/phases/06-google-maps-world-map/06-UI-SPEC.md §Motion Contract — markerPulse CSS animation trigger, useReducedMotion gate
  </read_first>
  <behavior>
    - ChapterCoord interface exported, fields: chapterIndex: number, lat: number, lng: number, label?: string — unchanged
    - ViewerMapProps interface: chapterCoords, activeChapterIndex?, pinPlacingForChapter?, onPinPlaced?, onCancelPinPlacing? — unchanged
    - Map renders when chapterCoords.length > 0 OR when isPlacing (pin-placing on empty map)
    - NoGpsState renders when chapterCoords.length === 0 AND !isPlacing
    - flyTo effect: useEffect on [activeChapterIndex, chapterCoords, isPlacing, map] → map.panTo + map.setZoom(10)
    - Marker pulse: active chapter's AdvancedMarker child div gets className "marker-pulse" when !shouldReduceMotion, no class when shouldReduceMotion
    - Click-to-place: Map onClick fires → e.detail.latLng?.lat and e.detail.latLng?.lng extracted → onPinPlaced called; pendingMarkerCoord state tracks the pending position for rendering the 14px pending AdvancedMarker
    - Polyline: only renders when chapterCoords.length > 1; path is chapterCoords sorted by chapterIndex mapped to {lat, lng}; icons array with SVG dash pattern; strokeOpacity: 0 on Polyline itself
  </behavior>
  <action>
    Completely replace the contents of src/components/viewer/ViewerMap.tsx. The new file structure:

    1. Header: 'use client' directive

    2. Imports:
       - { useEffect, useState } from 'react'
       - { APIProvider, Map, AdvancedMarker, Polyline, useMap, MapMouseEvent } from '@vis.gl/react-google-maps'
         Note: Do NOT import APIProvider here — it goes in CinematicViewer.tsx (Plan 03). Import only: Map, AdvancedMarker, Polyline, useMap, MapMouseEvent
       - { useReducedMotion } from 'framer-motion'
       - { NoGpsState } from '@/components/viewer/NoGpsState'

    3. Exported ChapterCoord interface — identical to current file

    4. ViewerMapProps interface — identical to current file

    5. ViewerMap function component with these state items:
       - announcement: string (useState '')
       - pendingCoord: { lat: number; lng: number } | null (useState null) — tracks pending pin position

    6. Derived values:
       - isPlacing: pinPlacingForChapter !== undefined
       - shouldReduceMotion: useReducedMotion()
       - map: useMap() — Google Maps instance
       - sortedCoords: chapterCoords sorted by chapterIndex

    7. Effect 1 — flyTo equivalent:
       useEffect on [activeChapterIndex, chapterCoords, isPlacing, map]
       Guard: if activeChapterIndex === undefined || !map || isPlacing return
       Find coord by chapterIndex === activeChapterIndex
       Call map.panTo({ lat: coord.lat, lng: coord.lng }) and map.setZoom(10)
       Call setAnnouncement('Map moved to ' + (coord.label ?? 'Chapter ' + (activeChapterIndex + 1)))

    8. Effect 2 — cursor crosshair in pin-placing mode:
       When isPlacing: no direct DOM manipulation needed — handle via style prop on the Map
       (Map onClick handles the pin placement; cursor is set via style={{ cursor: isPlacing ? 'crosshair' : undefined }})

    9. handleMapClick function (inline or useCallback):
       Receives e: MapMouseEvent
       If pinPlacingForChapter === undefined return
       lat = e.detail.latLng?.lat; lng = e.detail.latLng?.lng
       If lat == null || lng == null return
       setPendingCoord({ lat, lng })
       Call onPinPlaced?.(pinPlacingForChapter, lat, lng)

    10. Effect 3 — clear pending coord when leaving pin-placing mode:
       useEffect on [isPlacing]: if !isPlacing → setPendingCoord(null)

    11. JSX return — outer container div:
       role="region" aria-label="Trip route map" tabIndex={0}
       className: the exact 6-line joined Tailwind class string from Phase 4 (see Interfaces block above)

    12. Inner structure:
       <div className="relative w-full h-full">
         <Map
           defaultCenter={{ lat: sortedCoords[0]?.lat ?? 0, lng: sortedCoords[0]?.lng ?? 0 }}
           defaultZoom={8}
           colorScheme="DARK"
           disableDefaultUI={true}
           gestureHandling="greedy"
           clickableIcons={false}
           style={{ width: '100%', height: '100%', cursor: isPlacing ? 'crosshair' : undefined }}
           onClick={isPlacing ? handleMapClick : undefined}
         >
           {/* Chapter markers */}
           {sortedCoords.map((coord) => {
             const isActive = coord.chapterIndex === activeChapterIndex
             return (
               <AdvancedMarker key={coord.chapterIndex} position={{ lat: coord.lat, lng: coord.lng }}>
                 <div style={{
                   width: '8px', height: '8px', borderRadius: '50%', background: '#E5A663',
                 }} className={isActive && !shouldReduceMotion ? 'marker-pulse' : ''} />
               </AdvancedMarker>
             )
           })}

           {/* Dashed amber Polyline */}
           {sortedCoords.length > 1 && (
             <Polyline
               path={sortedCoords.map(c => ({ lat: c.lat, lng: c.lng }))}
               strokeColor="#E5A663"
               strokeOpacity={0}
               strokeWeight={2}
               icons={[{
                 icon: { path: 'M 0,-1 0,1', strokeOpacity: 1, scale: 2 },
                 offset: '0',
                 repeat: '12px',
               }]}
             />
           )}

           {/* Pending pin (click-to-place mode) */}
           {isPlacing && pendingCoord && (
             <AdvancedMarker position={pendingCoord}>
               <div style={{
                 width: '14px', height: '14px', borderRadius: '50%',
                 background: '#E5A663', border: '2px solid #fff',
                 boxShadow: '0 0 0 3px rgba(229,166,99,0.35)',
               }} />
             </AdvancedMarker>
           )}
         </Map>

         {/* NoGpsState fallback */}
         {chapterCoords.length === 0 && !isPlacing && <NoGpsState />}

         {/* Pin-placing overlay */}
         {isPlacing && (
           <div className="absolute top-3 inset-x-3 z-10 flex items-center justify-between gap-2 rounded-lg bg-ink/90 backdrop-blur-sm px-3 py-2 border border-amber-accent/30">
             <p className="font-sans text-[12px] text-amber-accent">
               Click map to place location pin
             </p>
             <button type="button" onClick={onCancelPinPlacing}
               className="font-sans text-[11px] uppercase tracking-[0.08em] text-parchment-400 hover:text-parchment-200">
               Cancel
             </button>
           </div>
         )}

         {/* sr-only live region */}
         <span className="sr-only" aria-live="polite" aria-atomic="true">{announcement}</span>
       </div>

    13. Export: export default ViewerMap (named export AND default export — matches current file)

    IMPORTANT: There is NO APIProvider in this file. APIProvider lives in CinematicViewer.tsx (Plan 03). ViewerMap uses useMap() which requires being a descendant of APIProvider — this is satisfied by Plan 03's wrapping.

    Do NOT include any mapboxgl import, mapboxgl.accessToken, mapbox-gl/dist/mapbox-gl.css import, or any mapboxgl.Map, mapboxgl.Marker, or mapboxgl.Popup references.
  </action>
  <verify>
    <automated>
      node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/viewer/ViewerMap.tsx','utf8'); const noMapbox=!s.includes('mapbox') && !s.includes('mapboxgl'); const hasGoogleMaps=s.includes('@vis.gl/react-google-maps'); const hasUseMap=s.includes('useMap()'); const hasChapterCoord=s.includes('export interface ChapterCoord'); const hasPolyline=s.includes('M 0,-1 0,1'); const hasColorScheme=s.includes('colorScheme'); process.exit((noMapbox&&hasGoogleMaps&&hasUseMap&&hasChapterCoord&&hasPolyline&&hasColorScheme) ? 0 : 1)"
    </automated>
  </verify>
  <acceptance_criteria>
    - ViewerMap.tsx does NOT contain any of: "mapbox", "mapboxgl", "mapbox-gl", "NEXT_PUBLIC_MAPBOX_TOKEN", "mapbox://styles"
    - ViewerMap.tsx contains "from '@vis.gl/react-google-maps'"
    - ViewerMap.tsx contains "useMap()"
    - ViewerMap.tsx contains "export interface ChapterCoord"
    - ViewerMap.tsx contains "colorScheme" with value "DARK"
    - ViewerMap.tsx contains "disableDefaultUI"
    - ViewerMap.tsx contains "gestureHandling"
    - ViewerMap.tsx contains "'M 0,-1 0,1'" (Polyline dash SVG path)
    - ViewerMap.tsx contains "repeat: '12px'" (Polyline dash repeat)
    - ViewerMap.tsx contains "'marker-pulse'" (CSS animation class reference)
    - ViewerMap.tsx contains "map.panTo" and "map.setZoom(10)"
    - ViewerMap.tsx contains "e.detail.latLng" (Google Maps click event accessor)
    - ViewerMap.tsx contains "role=\"region\" aria-label=\"Trip route map\""
    - ViewerMap.tsx contains "sr-only" aria-live region
    - ViewerMap.tsx contains "NoGpsState" import and usage
    - ViewerMap.tsx contains the exact responsive className string "h-[40dvh]" and "md:w-[38vw]"
    - ViewerMap.tsx does NOT contain APIProvider (APIProvider belongs in CinematicViewer.tsx)
  </acceptance_criteria>
  <done>ViewerMap.tsx fully rewritten with @vis.gl/react-google-maps. All Phase 4 behaviors preserved. No mapbox-gl references remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click → Map onClick | Map click event provides lat/lng from Google Maps; used directly for pin placement |
| NEXT_PUBLIC_GOOGLE_MAPS_API_KEY → browser | API key embedded in client bundle; must be HTTP-referrer-restricted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | e.detail.latLng in Map onClick | mitigate | Null-check lat and lng before calling onPinPlaced: if (lat == null || lng == null) return |
| T-06-04 | Information Disclosure | Google Maps API key visible in network requests | mitigate | Key restricted to HTTP referrers (D-13 — human-action in Plan 06-06); key exposed to all users of the site which is acceptable for Maps JS API |
| T-06-05 | Denial of Service | Unbounded Polyline icons array | accept | icons array has exactly 1 entry; chapterCoords are bounded by trip photo count (max ~12 per Phase 2) |
</threat_model>

<verification>
After ViewerMap.tsx is rewritten:

1. TypeScript check (no compile errors from the new file):
   pnpm exec tsc --noEmit --skipLibCheck

2. Grep for mapbox to confirm clean removal:
   grep -r "mapbox" src/components/viewer/ViewerMap.tsx — should return 0 matches

3. Confirm prop interface unchanged (CinematicViewer.tsx should not need any edits — just APIProvider wrap in Plan 03):
   grep "ChapterCoord" src/components/viewer/CinematicViewer.tsx — should still find the import
</verification>

<success_criteria>
- ViewerMap.tsx contains zero mapbox references
- All @vis.gl/react-google-maps imports resolve (package installed by Plan 01)
- ChapterCoord and ViewerMapProps interfaces are unchanged from Phase 4
- pnpm exec tsc --noEmit --skipLibCheck exits 0 for ViewerMap.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/06-google-maps-world-map/06-02-SUMMARY.md`
</output>
