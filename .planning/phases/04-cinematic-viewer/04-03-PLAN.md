---
phase: 04-cinematic-viewer
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - src/app/(app)/trip/[id]/page.tsx
  - src/app/(app)/layout.tsx
autonomous: false
requirements:
  - VIEW-01
  - VIEW-02
  - VIEW-03
user_setup: []

must_haves:
  truths:
    - "For trips with generation_status === 'completed', /trip/[id] renders ViewerNav, ScrollProgress, a vertical stack of ChapterSection elements (one per chapter), and a dynamically-imported ViewerMap"
    - "For trips with generation_status === 'draft' | 'generating' | 'failed', /trip/[id] renders the EXISTING Phase 3 layout unchanged (TripDetailHeader, TripPhotoGrid, StorySection/DraftStoryCTA/GeneratingStoryState)"
    - "GPS coordinates are extracted server-side: for each chapter, the first photo_id in chapter.photo_ids is looked up in the photos table (select id, latitude, longitude); chapters whose first photo has null latitude/longitude are omitted from chapterCoords"
    - "chapterCoords is a { chapterIndex: number; lat: number; lng: number; label?: string }[] passed as a plain prop to ViewerMap — raw GPS never appears in the photos payload sent to the browser (preserves Phase 2 security decision)"
    - "When ALL completed-trip chapters have null GPS, chapterCoords is [] and ViewerMap renders NoGpsState over the dark map canvas — layout is consistent (map panel still present per D-08)"
    - "AppNav from (app)/layout.tsx is suppressed on /trip/[id] when the trip is completed — ViewerNav replaces it; for non-completed trips on /trip/[id], the standard AppNav still renders"
    - "ViewerMap is imported via dynamic(() => import('@/components/viewer/ViewerMap'), { ssr: false }) — mapbox-gl uses browser APIs and cannot be server-rendered"
    - "Each ChapterSection receives a photoUrl derived from the public storage URL of chapter.photo_ids[0] (the same URL pattern as Phase 2 TripPhotoCell)"
    - "ChapterSection.onInView fires when the chapter enters the viewport at threshold 0.3, and the parent updates an activeChapterIndex state that is passed to ViewerMap — driving the per-chapter flyTo per D-04"
    - "`npm run build` passes cleanly with the cinematic viewer wired"
    - "A 375px viewport renders the viewer without horizontal scroll (success criterion 4)"
    - "Scrolling through a completed trip reveals each chapter section with photo parallax, fading narrative text, and the Mapbox map flying to each chapter's location (success criterion 1 + 2)"
  artifacts:
    - path: "src/app/(app)/trip/[id]/page.tsx"
      provides: "RSC that branches on generation_status: completed → cinematic viewer; draft/generating/failed → Phase 3 layout (unchanged)"
      contains: "generation_status === 'completed'"
    - path: "src/app/(app)/trip/[id]/page.tsx"
      provides: "Server-side GPS coordinate extraction per D-06"
      contains: "chapterCoords"
    - path: "src/app/(app)/trip/[id]/page.tsx"
      provides: "Dynamic Mapbox import per UI-SPEC"
      contains: "ssr: false"
    - path: "src/app/(app)/layout.tsx"
      provides: "AppNav suppression on cinematic viewer (completed-trip route)"
      contains: "AppNav"
  key_links:
    - from: "src/app/(app)/trip/[id]/page.tsx"
      to: "src/components/viewer/ChapterSection"
      via: "map over chapters renders ChapterSection per chapter"
      pattern: "ChapterSection"
    - from: "src/app/(app)/trip/[id]/page.tsx"
      to: "src/components/viewer/ViewerNav"
      via: "renders ViewerNav at viewer route root"
      pattern: "ViewerNav"
    - from: "src/app/(app)/trip/[id]/page.tsx"
      to: "src/components/viewer/ScrollProgress"
      via: "renders ScrollProgress at viewer route root"
      pattern: "ScrollProgress"
    - from: "src/app/(app)/trip/[id]/page.tsx"
      to: "src/components/viewer/ViewerMap"
      via: "dynamic import with ssr:false, receives chapterCoords + activeChapterIndex props"
      pattern: "dynamic\\(\\(\\) => import\\('@/components/viewer/ViewerMap'"
    - from: "src/app/(app)/trip/[id]/page.tsx"
      to: "supabase.photos.select('id, latitude, longitude')"
      via: "GPS coordinate extraction query"
      pattern: "select\\(['\"]id, latitude, longitude['\"]\\)"
    - from: "src/app/(app)/layout.tsx"
      to: "/trip/[id] route"
      via: "conditional AppNav render (suppressed for completed-trip viewer)"
      pattern: "AppNav"
---

<objective>
Wire the four viewer components (ChapterSection, ViewerNav, ScrollProgress, ViewerMap) from Plans 04-01 and 04-02 into `/trip/[id]/page.tsx`. Extend the RSC with server-side GPS coordinate extraction per D-06 + D-07. Suppress the standard AppNav from `(app)/layout.tsx` on the cinematic viewer route. Verify end-to-end via a human checkpoint and `npm run build`.

Purpose: This is the integration plan that turns the cinematic viewer from a set of components into a working surface. The build branches on `generation_status === 'completed'`: completed trips get the cinematic viewer; draft/generating/failed trips keep the Phase 3 layout exactly as it is today.

Output: Modified RSC + (app) layout, a working `/trip/[id]` cinematic viewer for completed trips, and a human-verified end-to-end pass (parallax + animated map + 375px responsiveness + brand visual system per VIEW-01/02/03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cinematic-viewer/04-CONTEXT.md
@.planning/phases/04-cinematic-viewer/04-UI-SPEC.md
@.planning/phases/04-cinematic-viewer/04-PATTERNS.md
@.planning/phases/04-cinematic-viewer/04-01-PLAN.md
@.planning/phases/04-cinematic-viewer/04-02-PLAN.md

@src/app/(app)/trip/[id]/page.tsx
@src/app/(app)/layout.tsx
@src/components/layout/AppNav.tsx
@src/components/trip/TripDetailHeader.tsx
@src/components/trip/TripPhotoGrid.tsx
@src/components/trip/StorySection.tsx
@src/components/trip/DraftStoryCTA.tsx
@src/components/trip/GeneratingStoryState.tsx
@src/types/database.ts

<interfaces>
<!-- From Plans 04-01 and 04-02 (will exist when this plan executes) -->

ChapterSection (04-01):
  props: { chapter: StoryChapter; chapterIndex: number; photoUrl: string; onInView?: (chapterIndex: number) => void }
  Client component. Imports useScroll/useTransform/useReducedMotion from framer-motion.

ViewerNav (04-01):
  props: none (back link is fixed to /dashboard)
  Client component. Renders fixed top z-40 ghost nav with scroll-fade.

ScrollProgress (04-01):
  props: none
  Client component. Renders fixed left-edge 3px amber bar driven by page useScroll().

ViewerMap (04-02):
  props: { chapterCoords: ChapterCoord[]; activeChapterIndex?: number }
  type ChapterCoord = { chapterIndex: number; lat: number; lng: number; label?: string }
  Client component, MUST be imported via dynamic(() => import('@/components/viewer/ViewerMap'), { ssr: false }) because mapbox-gl uses browser APIs.

NoGpsState (04-02):
  Rendered inside ViewerMap when chapterCoords.length === 0. Page does not render directly.

<!-- Existing RSC contract (carry forward unchanged) -->

src/app/(app)/trip/[id]/page.tsx:
  async server component
  Reads { params: { id: string } }
  Auth: supabase.auth.getUser() — notFound() if null
  Ownership: trips query with .eq('id', params.id).eq('user_id', user.id).single() — notFound() if null
  Photos: select * from photos where trip_id, ordered by order_index + created_at
  Chapters (when completed): select * from story_chapters where trip_id, ordered by chapter_index
  PhotoForDisplay shape (Phase 2): { id, publicUrl, taken_at, hasGps } — GPS stripped from client payload

<!-- Photo URL pattern (carry forward from existing page.tsx line 44) -->
publicUrl = supabase.storage.from('trip-photos').getPublicUrl(p.storage_path).data.publicUrl
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend /trip/[id] RSC — branch on completed, build chapterCoords, render cinematic viewer</name>
  <files>src/app/(app)/trip/[id]/page.tsx</files>
  <read_first>
    - src/app/(app)/trip/[id]/page.tsx (current state — preserve auth, ownership, photos query, chapter query exactly; only extend the render branch and add the GPS query)
    - .planning/phases/04-cinematic-viewer/04-PATTERNS.md (section: src/app/(app)/trip/[id]/page.tsx — full extension pattern including GPS coordinate extraction code, dynamic import, and completed-trip render branch)
    - .planning/phases/04-cinematic-viewer/04-UI-SPEC.md (section: ViewerPage RSC Extension — full spec including which Phase 3 components to suppress for completed trips)
    - .planning/phases/04-cinematic-viewer/04-CONTEXT.md (D-01, D-06, D-07, D-08 — viewer replaces /trip/[id]; server-side GPS extraction; chapterCoords shape; no-GPS fallback)
    - src/types/database.ts (StoryChapter type — fields photo_ids and chapter_index used in GPS extraction)
    - src/components/trip/TripDetailHeader.tsx, src/components/trip/TripPhotoGrid.tsx, src/components/trip/StorySection.tsx, src/components/trip/DraftStoryCTA.tsx, src/components/trip/GeneratingStoryState.tsx (confirm imports and ensure draft/generating/failed branch continues to use them unchanged)
    - .planning/phases/04-cinematic-viewer/04-01-PLAN.md and 04-02-PLAN.md (component contracts — ChapterSection, ViewerNav, ScrollProgress, ViewerMap props)
  </read_first>
  <action>
    Edit `src/app/(app)/trip/[id]/page.tsx`. Preserve the entire existing auth + ownership + photos + chapters fetch logic — the file is async server component and stays that way.

    Add imports at the top alongside existing imports:
    - `import dynamic from 'next/dynamic'`
    - `import { ChapterSection } from '@/components/viewer/ChapterSection'`
    - `import { ViewerNav } from '@/components/viewer/ViewerNav'`
    - `import { ScrollProgress } from '@/components/viewer/ScrollProgress'`

    Add (top-level, outside the page function) the dynamic Mapbox import:
    - `const ViewerMap = dynamic(() => import('@/components/viewer/ViewerMap'), { ssr: false })`
    Note: a Next.js 14 RSC file can hold a `dynamic()` call only when the dynamic component is consumed inside a server component without `ssr: false`. Because `ssr: false` is incompatible with server components in Next 14, the cinematic viewer return value must be wrapped in a `'use client'` boundary OR the dynamic import must be hoisted into a small client wrapper component. Implement the cleanest approach: create the dynamic import inside a small client wrapper component, OR keep page.tsx as RSC and render an inline client boundary.

    Resolution path (choose ONE and document in SUMMARY):
    **Option A (preferred — minimal client surface):** Create a tiny client wrapper component `src/components/viewer/CinematicViewer.tsx` (`'use client'`) that:
      - Accepts props: `{ chapters: StoryChapter[]; photoUrlByChapter: Record<string, string>; chapterCoords: ChapterCoord[] }`
      - Holds React `useState<number | undefined>(undefined)` for `activeChapterIndex`
      - Renders `<ViewerNav />`, `<ScrollProgress />`, the chapter section stack, and the dynamically-imported `<ViewerMap chapterCoords={...} activeChapterIndex={...} />`
      - Calls the `dynamic(() => import('@/components/viewer/ViewerMap'), { ssr: false })` import at module scope inside this file
      - Each ChapterSection receives `onInView={(idx) => setActiveChapterIndex(idx)}` so map flyTo is driven by scroll
    Then `page.tsx` (RSC) renders `<CinematicViewer chapters={chapters} photoUrlByChapter={...} chapterCoords={chapterCoords} />`.

    **Option B (inline — page.tsx renders ChapterSection directly, but cannot use dynamic+ssr:false at RSC top-level):** rejected.

    Use Option A.

    Add server-side GPS extraction logic — insert AFTER the existing `chapters = chapterRows ?? []` assignment, BEFORE the return statement. Build `chapterCoords` per the PATTERNS.md template:
    1. Declare type local to the file: `type ChapterCoord = { chapterIndex: number; lat: number; lng: number; label?: string }`
    2. Initialize `let chapterCoords: ChapterCoord[] = []`
    3. Only run when `trip.generation_status === 'completed' && chapters.length > 0`:
       - Build `firstPhotoIds = chapters.map((ch) => ch.photo_ids?.[0]).filter(Boolean) as string[]`
       - If `firstPhotoIds.length > 0`: query `supabase.from('photos').select('id, latitude, longitude').in('id', firstPhotoIds)` — IMPORTANT: this query is the ONLY place raw GPS leaves the database; the resulting coordinates are mapped into the `chapterCoords` tuple and the raw `photos` query response is NOT serialized to the client (it stays in this RSC scope and is discarded).
       - Build a `Map<string, { lat: number; lng: number }>` keyed by photo id, filtering out rows where `latitude === null || longitude === null`
       - Iterate `chapters` and produce `chapterCoords` by looking up `chapter.photo_ids[0]`'s coord — if no coord found for a chapter, OMIT that chapter from chapterCoords (do not push null)
       - If after filtering `chapterCoords.length === 0`, the variable stays `[]` and ViewerMap will render NoGpsState per D-08

    Also build a `photoUrlByChapter: Record<string, string>` map for the chapter-section photos: for each chapter, derive the public URL of `chapter.photo_ids[0]` from the existing `photoRows` data (use the same `supabase.storage.from('trip-photos').getPublicUrl(p.storage_path).data.publicUrl` pattern as the existing PhotoForDisplay projection). Key the map by `chapter.id`. Pass into CinematicViewer so the client component does not need to make its own URL lookups.

    Modify the return statement to branch on `generation_status`:
    1. **`trip.generation_status === 'completed' && chapters.length > 0`** (NEW branch):
       Render `<CinematicViewer chapters={chapters} photoUrlByChapter={photoUrlByChapter} chapterCoords={chapterCoords} />`.
       Do NOT render `TripDetailHeader`, `TripPhotoGrid`, `StorySection` in this branch — the cinematic viewer takes over the entire route.
       Do NOT render the existing wrapper `<div className="mx-auto max-w-[1280px] ...">` for this branch — cinematic viewer is full-bleed.
    2. **`trip.generation_status === 'completed' && chapters.length === 0`** (edge case — completed but no chapters):
       Fall through to the legacy Phase 3 render (extremely rare; defensive).
    3. **All other states (draft, generating, failed, AND the chapters===0 edge case above)**: render the EXISTING Phase 3 markup unchanged — preserve lines 60–82 of the current file exactly (TripDetailHeader + TripPhotoGrid + DraftStoryCTA/GeneratingStoryState/StorySection logic).

    Preserve the Phase 2 security decision: the existing `PhotoForDisplay[]` mapping does NOT include latitude/longitude. Do not change that projection. The NEW `photos.select('id, latitude, longitude')` query result is consumed inside the RSC and only the derived `chapterCoords` tuples (no photo ids, no extra fields) are passed to the client per D-06.

    Do NOT add `'use client'` to page.tsx — it remains an async server component. All client behavior lives in CinematicViewer.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "trip/\[id\]/page|viewer/CinematicViewer" ; grep -c "ChapterSection\|CinematicViewer" src/app/\(app\)/trip/\[id\]/page.tsx ; grep -c "chapterCoords" src/app/\(app\)/trip/\[id\]/page.tsx ; grep -c "select('id, latitude, longitude')" src/app/\(app\)/trip/\[id\]/page.tsx ; grep -c "generation_status === 'completed'" src/app/\(app\)/trip/\[id\]/page.tsx ; grep -c "DraftStoryCTA\|GeneratingStoryState" src/app/\(app\)/trip/\[id\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/trip/[id]/page.tsx` remains an `async` function (server component) — does NOT contain `'use client'`
    - File contains the new query `supabase.from('photos').select('id, latitude, longitude').in('id', firstPhotoIds)` (or equivalent that selects exactly id+latitude+longitude)
    - File constructs a `chapterCoords` array of `{ chapterIndex, lat, lng }` tuples
    - File contains a branch on `trip.generation_status === 'completed'` that renders the cinematic viewer (either inline or via CinematicViewer wrapper)
    - File contains the import or render reference for CinematicViewer (Option A) — `src/components/viewer/CinematicViewer.tsx` exists and is imported here
    - File preserves the existing draft/generating/failed render branch — `DraftStoryCTA`, `GeneratingStoryState`, `TripDetailHeader`, `TripPhotoGrid` are still imported and rendered for non-completed trips
    - The completed-trip branch does NOT render `TripDetailHeader`, `TripPhotoGrid`, or `StorySection` (those are Phase 3 components replaced by the cinematic viewer)
    - The existing `PhotoForDisplay` projection retains its lean shape (id, publicUrl, taken_at, hasGps) — no latitude/longitude added to the client-facing photos array
    - The new GPS query result is consumed inside the RSC scope and not serialized to the client
    - `src/components/viewer/CinematicViewer.tsx` exists, starts with `'use client'`, holds the `activeChapterIndex` state, holds the `dynamic(() => import('@/components/viewer/ViewerMap'), { ssr: false })` import, and renders `<ViewerNav />` + `<ScrollProgress />` + chapter section stack + `<ViewerMap chapterCoords={...} activeChapterIndex={...} />`
    - Each ChapterSection in CinematicViewer receives `onInView={(idx) => setActiveChapterIndex(idx)}` so map flyTo is driven by scroll
    - `npx tsc --noEmit` produces no errors
  </acceptance_criteria>
  <done>
    /trip/[id] renders the cinematic viewer for completed trips and the unchanged Phase 3 layout for all other states. GPS coordinates are extracted server-side and passed to the map as plain tuples — no raw GPS in the client photo payload.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Suppress AppNav on cinematic viewer — modify (app)/layout.tsx to skip AppNav when route is /trip/[id] with completed trip</name>
  <files>src/app/(app)/layout.tsx</files>
  <read_first>
    - src/app/(app)/layout.tsx (current state — renders AppNav unconditionally; need conditional render or alternate layout segment)
    - src/components/layout/AppNav.tsx (AppNav signature — accepts userEmail prop)
    - .planning/phases/04-cinematic-viewer/04-UI-SPEC.md (section: ViewerNav — explicit note that AppNav must be suppressed on the cinematic route; lists implementation approach options)
    - .planning/phases/04-cinematic-viewer/04-PATTERNS.md (section: src/app/(app)/trip/[id]/page.tsx — "Key integration note" about AppNav suppression options)
    - .planning/phases/04-cinematic-viewer/04-CONTEXT.md (D-02 — ghost floating nav replaces AppNav)
  </read_first>
  <action>
    Modify `src/app/(app)/layout.tsx` to conditionally suppress `<AppNav>` when the cinematic viewer is being rendered. The layout is currently an async server component that runs `supabase.auth.getUser()` and redirects unauthenticated visitors.

    Choose ONE of these two implementation approaches — document the choice in the SUMMARY:

    **Approach 1 (preferred — z-index layering, simplest):**
    Keep `(app)/layout.tsx` exactly as it is — AppNav still renders unconditionally. The ViewerNav from Plan 04-01 uses `z-40` and `position: fixed`, which sits ON TOP of AppNav (which uses `z-30 sticky`). Visually, ViewerNav covers AppNav completely. The cost: AppNav still mounts (a tiny DOM cost) but is never visible on the cinematic route. The benefit: zero risk of breaking the dashboard/wizard nav behavior.

    Test under this approach: scroll the cinematic viewer and verify ViewerNav covers AppNav at all scroll positions (because ViewerNav is `fixed top-0 z-40` and AppNav is `sticky top-0 z-30`, ViewerNav always wins the stacking contest). On completed trips, the user sees only ViewerNav. On draft/generating/failed trips on the same `/trip/[id]` route, AppNav remains visible because CinematicViewer (and hence ViewerNav) does not mount.

    If Approach 1 is chosen, NO modification to `(app)/layout.tsx` is needed. The layout file is unchanged. This is the simplest path and is what the UI-SPEC permits ("render ViewerNav absolutely on top (z-40) so it visually replaces AppNav").

    **Approach 2 (clean separation — route-aware layout):**
    Make `(app)/layout.tsx` a client component using `usePathname()` to detect `/trip/[id]` routes and conditionally render AppNav. The complication: the current layout reads auth via the server-side `createClient()` — converting it to a client component would require either (a) wrapping AppNav in a separate client component that uses `usePathname()` and renders null on /trip/[id], or (b) creating a parallel route group `(viewer)` for the cinematic route. (a) is the lower-risk path; (b) is a deeper restructure.

    If choosing Approach 2 (a): create `src/components/layout/AppNavWrapper.tsx` (`'use client'`) that uses `usePathname()`. When `pathname.startsWith('/trip/')`, render nothing (or render a hidden node — but rendering nothing is cleaner). Otherwise, render `<AppNav userEmail={userEmail} />`. Modify `(app)/layout.tsx` to import `AppNavWrapper` instead of `AppNav`. The wrapper does not know whether the trip is completed (that lives server-side); it suppresses AppNav for the entire `/trip/[id]` route — which is fine because the cinematic viewer always covers the nav with ViewerNav for completed trips, and for draft/generating/failed trips on `/trip/[id]`, the absence of AppNav is the only downside. Document this in SUMMARY as a known trade-off.

    **Decision rule for the executor:** Pick Approach 1 (no layout changes) UNLESS a verification step reveals AppNav is bleeding through the ViewerNav (which it should not, given z-index). If Approach 1 works, document in SUMMARY: "Approach 1 chosen — no changes to (app)/layout.tsx; ViewerNav z-40 covers AppNav z-30." If verification shows bleed-through, fall back to Approach 2(a) — create AppNavWrapper.tsx and modify (app)/layout.tsx to use it.

    Regardless of approach: the user can ALWAYS navigate back to `/dashboard` via either AppNav (when visible) or the back link in ViewerNav — there is always an exit path per D-02.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "\\(app\\)/layout" ; npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Either: `src/app/(app)/layout.tsx` is UNCHANGED (Approach 1 — relying on ViewerNav z-40 stacking above AppNav z-30), with the choice documented in SUMMARY
    - Or: `src/app/(app)/layout.tsx` is modified to use a `src/components/layout/AppNavWrapper.tsx` client component that calls `usePathname()` and conditionally returns null for `/trip/` routes (Approach 2(a)), with the choice documented in SUMMARY
    - If Approach 2(a) is taken: `src/components/layout/AppNavWrapper.tsx` starts with `'use client'`, imports `usePathname` from `next/navigation`, imports `AppNav` from `./AppNav`, and exports a function that returns `null` when `pathname` starts with `/trip/` and `<AppNav userEmail={props.userEmail} />` otherwise
    - `(app)/layout.tsx` still runs `supabase.auth.getUser()` and redirects to `/login` if user is null (security invariant — do not weaken)
    - `npx tsc --noEmit` produces no errors
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>
    Either ViewerNav cleanly covers AppNav via z-index (preferred), or AppNav is suppressed on /trip/ routes via the client wrapper. Either way, the cinematic viewer presents only ViewerNav, and the security/auth invariants of (app)/layout.tsx are preserved.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human end-to-end verification — cinematic viewer on a completed trip across desktop and 375px mobile</name>
  <files>(verification only — no file changes)</files>
  <read_first>
    - .planning/phases/04-cinematic-viewer/04-UI-SPEC.md (success criteria implicit in all sections)
    - .planning/ROADMAP.md (Phase 4 §Success criteria — 4 criteria to verify)
    - .planning/phases/04-cinematic-viewer/04-CONTEXT.md (D-02, D-04, D-08 — behaviors to verify)
  </read_first>
  <what-built>
    The cinematic viewer is wired end-to-end:
    - `/trip/[id]` renders ChapterSection (parallax photo + scroll-linked text fade), ViewerNav (ghost floating nav), ScrollProgress (left-edge amber hairline), and ViewerMap (dynamically imported Mapbox map with dashed amber path + amber dot markers + per-chapter flyTo) for trips with `generation_status === 'completed'`
    - Draft/generating/failed trips on `/trip/[id]` continue to render the Phase 3 layout (TripDetailHeader, TripPhotoGrid, StorySection/DraftStoryCTA/GeneratingStoryState) unchanged
    - GPS coordinates are extracted server-side and passed as `{ chapterIndex, lat, lng }[]` tuples — raw GPS is not in the client photo payload
    - When all chapters lack GPS, the map panel still mounts (dark-v11 canvas) and NoGpsState shows "No location data" centered
    - `npm run build` passes
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev` (port 3000).
    2. Log in (magic link via Supabase) and navigate to a trip with `generation_status = 'completed'` and at least 3 photos with GPS (you can verify GPS in the DB or via the trip detail badges from Phase 2).
    3. **Success Criterion 1 — Scroll-driven parallax + narrative motion:**
       - Confirm the page renders as a series of full-screen sections (NOT the Phase 3 photo grid + chapter list).
       - Scroll slowly through the first chapter. Confirm the photo backdrop translates vertically (parallax) and the chapter title + narrative fade IN as the chapter approaches center, then fade OUT as it exits — no manual click to advance chapters.
       - Confirm each chapter has its CHAPTER 01 / CHAPTER 02 eyebrow above the title.
       - Open DevTools, enable `prefers-reduced-motion: reduce`, reload — confirm text is fully visible immediately on each chapter (no fade), parallax is disabled (photo is static at scale-110), and ScrollProgress shows as a static low-opacity amber bar.
    4. **Success Criterion 2 — Animated Mapbox map with path:**
       - Confirm the Mapbox map is visible on the right (desktop) — dark canvas, amber dashed line connecting the chapter coordinates, small amber dot markers at each chapter location.
       - Scroll through each chapter. Confirm the map flies (`flyTo`) to the chapter's GPS location as that chapter enters the viewport (~30% threshold). The map should fly even with reduced-motion enabled (essential: true is mandatory for spatial navigation).
    5. **Success Criterion 3 — Brand visual system:**
       - Confirm the canvas is `#0A0A0A` ink (not pure black).
       - Confirm chapter titles use Fraunces (serif) and narrative prose uses Inter (sans).
       - Confirm amber `#E5A663` is used ONLY on: scroll-progress bar, map path line, map markers, and the nav-accent-line on ViewerNav (max 4 amber elements simultaneously).
       - Confirm NO amber on chapter titles, eyebrows, or prose.
       - Confirm grain overlay is visible (subtle film grain throughout).
    6. **Success Criterion 4 — 375px viewport usability:**
       - Open DevTools, switch device toolbar to 375px width (iPhone SE).
       - Confirm: no horizontal scroll; the map panel moves to a bottom sheet (h-[40dvh]); chapter narrative text is readable at 15–16px; ChevronLeft back button (icon only — no "Back to trips" text label on mobile) is visible top-left.
    7. **D-02 — Ghost nav behavior:**
       - Scroll to the top — ViewerNav is fully visible (opacity 1.0).
       - Scroll down past 180px — ViewerNav fades to near-invisible (opacity 0.08).
       - Scroll UP from a deep position — ViewerNav reappears at opacity 0.85 (not full 1.0).
       - Click the ChevronLeft back button — confirm navigation to `/dashboard`.
    8. **D-08 — No-GPS fallback (optional but recommended):**
       - Find or temporarily create a trip whose chapter photos all have null `latitude`/`longitude` in the DB (or temporarily comment out the GPS query result in dev to force chapterCoords=[]).
       - Reload the viewer for that trip. Confirm: the map panel still renders (dark canvas), no markers, no path, and "No location data" with a MapPin icon is centered in the map panel.
    9. **Non-completed trip regression:**
       - Navigate to a trip with `generation_status = 'draft'`. Confirm: the OLD Phase 3 layout renders (TripDetailHeader, photo grid, DraftStoryCTA) — NOT the cinematic viewer. Standard AppNav is visible (either always — Approach 1 — or via the wrapper — Approach 2(a)).
       - Navigate to a trip with `generation_status = 'generating'` if available. Confirm: GeneratingStoryState renders with the polling progress bar from Phase 3.
    10. **Build verification:**
       - In a terminal, run `npm run build` (or `npm.cmd run build` on Windows). Confirm exit code 0, no TypeScript errors, no build warnings related to viewer components.
    11. **Optional polish checks:**
       - Tab through the page with the keyboard. Confirm the back button receives a visible amber focus ring. Confirm the map container is focusable (tabIndex={0}).
       - Inspect the network panel — confirm `mapbox-gl` is loaded as a separate chunk (dynamic import) and not in the main `/trip/[id]` bundle.

    Report any deviations from the spec or any criterion that fails. Approve only if ALL of 3-7, 10, and 11 (where applicable) pass.
  </how-to-verify>
  <resume-signal>Type "approved" when all success criteria pass. Otherwise, describe each failing check and which UI-SPEC section it violates so the executor can patch.</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` exits 0 with no TypeScript or build errors related to viewer components or the RSC extension
- `npx tsc --noEmit` passes cleanly
- All four success criteria from ROADMAP §Phase 4 verified via the human checkpoint (parallax + narrative on scroll, animated Mapbox map with path, brand visual system, 375px usability)
- D-02 (ghost nav fade + back link), D-04 (per-chapter flyTo), D-06 (server-side GPS), D-07 (chapterCoords shape), D-08 (no-GPS fallback) all verified in the checkpoint
- Phase 3 layout for draft/generating/failed trips is preserved unchanged — regression check passes
- Raw GPS does not appear in any client-facing photo payload (Phase 2 security invariant preserved)
</verification>

<success_criteria>
- VIEW-01: Scrolling through `/trip/[id]` for a completed trip reveals each chapter with parallax photo backdrop and narrative text appearing in motion — no manual navigation. Verified.
- VIEW-02: An animated Mapbox map is visible and draws a dashed amber path connecting chapter GPS locations in chronological order. Verified.
- VIEW-03: Viewer renders exclusively in the brand visual system: ink canvas, parchment text, amber accent (reserved to 4 elements), Fraunces headlines, Inter body. Verified.
- 375px viewport: usable without horizontal scroll; map switches to bottom sheet. Verified.
- `npm run build` passes; `npx tsc --noEmit` passes.
- Phase 3 draft/generating/failed states preserved unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cinematic-viewer/04-03-SUMMARY.md` listing:
- Files modified
- Which AppNav-suppression approach was chosen (Approach 1 z-index layering vs Approach 2(a) AppNavWrapper) and why
- Whether the marker pulse polish from Plan 04-02 was implemented or deferred
- Any deviations from UI-SPEC discovered during integration
- Human verification outcome (approved / list of issues)
</output>
