---
phase: 04-cinematic-viewer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/viewer/ChapterSection.tsx
  - src/components/viewer/ViewerNav.tsx
  - src/components/viewer/ScrollProgress.tsx
autonomous: true
requirements:
  - VIEW-01
  - VIEW-03
user_setup: []

must_haves:
  truths:
    - "A ChapterSection client component exists that fills the viewport (min-height: 100dvh) with a parallax photo backdrop and a fading chapter text zone"
    - "Photo parallax uses useScroll target=sectionRef with offset ['start end','end start'] and the spec-locked useTransform ranges from UI-SPEC Motion Contract"
    - "Chapter text fade-in uses textOpacity = useTransform(scrollYProgress, [0.1, 0.3, 0.7, 0.9], [0, 1, 1, 0]) and textY = useTransform(scrollYProgress, [0.1, 0.3], [40, 0])"
    - "ChapterSection eyebrow renders 'CHAPTER 01', 'CHAPTER 02' format using String(index + 1).padStart(2, '0') — carrying forward the Phase 3 StoryChapterBlock convention"
    - "Atmospheric ambient-glow renders only when chapterIndex === 0 (first chapter only) — subsequent chapters have no glow per UI-SPEC"
    - "ViewerNav is a ghost floating nav at position fixed, z-40, that fades from opacity 1.0 (scrollY 0–80px) to 0.08 (scrollY ≥ 180px) and reverses to 0.85 on scroll-up"
    - "ScrollProgress is a fixed left-edge 3px amber bar (background #E5A663) driven by scaleY = scrollYProgress from page-level useScroll()"
    - "All three components gate Framer Motion animations on useReducedMotion() with the reduced-motion behaviors specified in UI-SPEC"
    - "All three components are exported and import without errors"
  artifacts:
    - path: "src/components/viewer/ChapterSection.tsx"
      provides: "Full-screen parallax chapter section component"
      contains: "useScroll"
    - path: "src/components/viewer/ChapterSection.tsx"
      provides: "Spec-locked scroll-linked text fade transforms"
      contains: "useTransform(scrollYProgress, [0.1, 0.3, 0.7, 0.9], [0, 1, 1, 0])"
    - path: "src/components/viewer/ViewerNav.tsx"
      provides: "Ghost floating nav with scroll-fade behavior"
      contains: "ChevronLeft"
    - path: "src/components/viewer/ScrollProgress.tsx"
      provides: "Fixed left-edge amber scroll-progress hairline"
      contains: "scrollYProgress"
  key_links:
    - from: "src/components/viewer/ChapterSection.tsx"
      to: "framer-motion"
      via: "useScroll + useTransform + useReducedMotion imports"
      pattern: "import .* useScroll, useTransform, useReducedMotion .* from 'framer-motion'"
    - from: "src/components/viewer/ChapterSection.tsx"
      to: "next/image"
      via: "fill+object-cover photo backdrop (carry forward from TripCard)"
      pattern: "import Image from 'next/image'"
    - from: "src/components/viewer/ChapterSection.tsx"
      to: "src/types/database (StoryChapter)"
      via: "typed chapter prop"
      pattern: "StoryChapter"
    - from: "src/components/viewer/ViewerNav.tsx"
      to: "/dashboard"
      via: "Back link href"
      pattern: "href=[\"']/dashboard[\"']"
    - from: "src/components/viewer/ScrollProgress.tsx"
      to: "framer-motion useScroll"
      via: "page-level scrollYProgress drives scaleY"
      pattern: "scaleY: scrollYProgress"
---

<objective>
Create the three viewer-chrome client components — `ChapterSection`, `ViewerNav`, `ScrollProgress` — that compose the static visual surfaces of the cinematic viewer. No Mapbox in this plan (Plan 04-02 owns Mapbox in isolation). No `/trip/[id]/page.tsx` changes (Plan 04-03 owns RSC wiring).

Purpose: Build the parallax + ghost-nav + scroll-progress chrome that the cinematic viewer composes. These three components are spec-locked from `04-UI-SPEC.md` Motion Contract and Component Specifications.

Output: Three new client components under `src/components/viewer/` with no external runtime dependencies beyond Framer Motion, Next/Image, and Lucide React (all already installed). Plans 04-02 and 04-03 build on top.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-cinematic-viewer/04-CONTEXT.md
@.planning/phases/04-cinematic-viewer/04-UI-SPEC.md
@.planning/phases/04-cinematic-viewer/04-PATTERNS.md

@src/components/trip/StoryChapterBlock.tsx
@src/components/layout/AppNav.tsx
@src/types/database.ts
@src/app/globals.css

<interfaces>
<!-- Spec-locked from UI-SPEC.md §Motion Contract — Phase 4 scroll-linked motion constants -->
<!-- DO NOT alter input/output ranges. -->

From src/types/database.ts:
StoryChapter { id, trip_id, chapter_index, title, narrative, photo_ids[], location_name, created_at }

Per-chapter photo parallax (useScroll target=sectionRef, offset=['start end', 'end start']):
  parallaxY = useTransform(scrollYProgress, [0, 1], ["-10%", "10%"])

Per-chapter text fade and rise (same scrollYProgress):
  textOpacity = useTransform(scrollYProgress, [0.1, 0.3, 0.7, 0.9], [0, 1, 1, 0])
  textY       = useTransform(scrollYProgress, [0.1, 0.3], [40, 0])

Paragraph stagger:
  parent.transition: { staggerChildren: 0.08, delayChildren: 0.05 }
  item: opacity 0->1, y 20->0, duration 0.7, ease [0.22, 1, 0.36, 1]

ViewerNav opacity (page-level scroll):
  scrollY 0-80px:    opacity = 1.0
  scrollY 80-180px:  linear remap 1.0 -> 0.08
  scrollY >= 180px:  opacity = 0.08
  scroll-up:         opacity -> 0.85 (NOT 1.0)
  CSS transition: opacity 300ms cubic-bezier(0.22, 1, 0.36, 1)

ScrollProgress: page-level useScroll() — scrollYProgress drives scaleY (Option A from UI-SPEC, preferred).

Eyebrow format (carry forward from Phase 3 StoryChapterBlock):
  `CHAPTER ${String(index + 1).padStart(2, '0')}`

Brand easing curve:
  [0.22, 1, 0.36, 1]  // cubic-bezier — ease-trailfilm
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create ChapterSection — full-screen parallax section with scroll-linked text fade</name>
  <files>src/components/viewer/ChapterSection.tsx</files>
  <read_first>
    - .planning/phases/04-cinematic-viewer/04-UI-SPEC.md (sections: ChapterSection spec, Motion Contract scroll-linked constants, Chapter Paragraph Stagger, Reduced Motion table)
    - .planning/phases/04-cinematic-viewer/04-PATTERNS.md (section: ChapterSection — section structure JSX, imports, paragraph stagger variants)
    - src/components/trip/StoryChapterBlock.tsx (Phase 3 analog — useInView pattern, eyebrow format String(index+1).padStart(2,'0'), useReducedMotion gate, brand easing [0.22, 1, 0.36, 1])
    - src/components/trip/TripCard.tsx (Image fill + object-cover + sizes pattern for full-bleed photo)
    - src/types/database.ts (StoryChapter interface — chapter prop type)
    - src/app/globals.css (.gradient-fade-bottom, .gradient-fade-top, .ambient-glow utility classes already defined — verify present before referencing)
  </read_first>
  <action>
    Create `src/components/viewer/ChapterSection.tsx` as a `'use client'` component. Props: `{ chapter: StoryChapter; chapterIndex: number; photoUrl: string; onInView?: (chapterIndex: number) => void }`. Imports: `useRef` from react; `motion`, `useScroll`, `useTransform`, `useInView`, `useReducedMotion` from `framer-motion`; `Image` from `next/image`; type `StoryChapter` from `@/types/database`.

    Inside the component: hold `sectionRef = useRef<HTMLElement>(null)`. Call `const { scrollYProgress } = useScroll({ target: sectionRef, offset: ['start end', 'end start'] })`. Define the three spec-locked motion values exactly: `parallaxY = useTransform(scrollYProgress, [0, 1], ['-10%', '10%'])`, `textOpacity = useTransform(scrollYProgress, [0.1, 0.3, 0.7, 0.9], [0, 1, 1, 0])`, `textY = useTransform(scrollYProgress, [0.1, 0.3], [40, 0])`. Use `useReducedMotion()` into `shouldReduce`. Use `useInView(sectionRef, { once: true, margin: '-120px' })` into `isInView` — used ONLY for the reduced-motion paragraph reveal fallback per UI-SPEC. Use `useInView(sectionRef, { once: false, amount: 0.3 })` into `isMapActiveView` and call `onInView?.(chapterIndex)` from a `useEffect` when `isMapActiveView` becomes true (this is what Plan 04-03 wires to ViewerMap flyTo per D-04 — keep callback contract here so Plan 04-03 can wire without modifying ChapterSection).

    Compute `eyebrow = `CHAPTER ${String(chapterIndex + 1).padStart(2, '0')}`` (carry the Phase 3 StoryChapterBlock format exactly).

    Render a `<section ref={sectionRef} aria-label={chapter.title} className="relative overflow-hidden min-h-[100dvh] bg-ink">`. Inside, in z-order:
    1. A `<motion.div className="absolute inset-0 scale-110" style={{ y: shouldReduce ? '0%' : parallaxY }}>` wrapping `<Image src={photoUrl} alt={chapter.title} fill className="object-cover object-center" priority={chapterIndex === 0} sizes="100vw" />`. (`scale-110` prevents parallax edge reveal per CONTEXT §Specifics.)
    2. `<div className="gradient-fade-top absolute inset-x-0 top-0 h-[30%] z-[5]" aria-hidden="true" />`
    3. `<div className="gradient-fade-bottom absolute inset-x-0 bottom-0 h-[60%] z-[5]" aria-hidden="true" />`
    4. If `chapterIndex === 0`: `<div className="ambient-glow absolute inset-0 z-[3] pointer-events-none" aria-hidden="true" />` (atmospheric glow on opening chapter only — subsequent chapters have NO atmospheric layer per UI-SPEC).
    5. Text content zone: `<motion.div className="absolute bottom-0 left-0 z-10 px-8 pb-16 md:px-16 md:pb-24 max-w-full md:max-w-[480px]" style={shouldReduce ? { opacity: 1, y: 0 } : { opacity: textOpacity, y: textY }}>`. Inside, in order: eyebrow `<p className="font-sans text-[11px] font-normal uppercase tracking-[0.18em] text-parchment-600 mb-1">{eyebrow}</p>`, title `<h2 className="font-serif font-medium text-2xl md:text-3xl leading-tight text-balance text-parchment mt-1">{chapter.title}</h2>`, narrative wrapper `<motion.div variants={paragraphContainer} initial="hidden" animate={shouldReduce ? (isInView ? 'visible' : 'hidden') : 'visible'} className="mt-3">` containing a single `<motion.p variants={paragraphItem} className="max-w-[62ch] font-sans text-base font-normal leading-[1.75] text-parchment-200">{chapter.narrative}</motion.p>`.

    Define the variants outside the JSX: `paragraphContainer = { hidden: {}, visible: { transition: { staggerChildren: 0.08, delayChildren: 0.05 } } }` and `paragraphItem = { hidden: { opacity: 0, y: shouldReduce ? 0 : 20 }, visible: { opacity: 1, y: 0, transition: { duration: shouldReduce ? 0 : 0.7, ease: [0.22, 1, 0.36, 1] } } }`.

    Color tokens MUST be exact: `bg-ink` (#0A0A0A), `text-parchment` (#FAFAF7), `text-parchment-200` (#E8E6DF), `text-parchment-600` (#6B6862). No amber on chapter text. Tailwind arbitrary values: `min-h-[100dvh]` (NOT 100vh — iOS Safari viewport bug per ROADMAP), `max-w-[480px]`, `max-w-[62ch]`, `leading-[1.75]`, `tracking-[0.18em]`, `text-[11px]`. Use `text-balance` (Tailwind v3.4+) on the h2.

    No fenced code blocks, no inline implementation comments beyond brief intent labels.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "viewer/ChapterSection" ; grep -c "useTransform(scrollYProgress, \[0.1, 0.3, 0.7, 0.9\], \[0, 1, 1, 0\])" src/components/viewer/ChapterSection.tsx ; grep -c "useTransform(scrollYProgress, \[0\], \[\"-10%\", \"10%\"\])\|useTransform(scrollYProgress, \[0, 1\], \['-10%', '10%'\])\|useTransform(scrollYProgress, \[0, 1\], \[\"-10%\", \"10%\"\])" src/components/viewer/ChapterSection.tsx ; grep -c "min-h-\[100dvh\]" src/components/viewer/ChapterSection.tsx ; grep -c "useReducedMotion" src/components/viewer/ChapterSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/viewer/ChapterSection.tsx` exists and starts with `'use client'`
    - File contains the literal string `useTransform(scrollYProgress, [0.1, 0.3, 0.7, 0.9], [0, 1, 1, 0])` (spec-locked text-fade transform)
    - File contains a parallax transform mapping `[0, 1]` to `['-10%', '10%']` for `parallaxY`
    - File contains `useTransform(scrollYProgress, [0.1, 0.3], [40, 0])` for `textY`
    - File contains the eyebrow expression `String(chapterIndex + 1).padStart(2, '0')` so the eyebrow renders `CHAPTER 01`, `CHAPTER 02`, etc.
    - File contains `useScroll({ target: sectionRef, offset: ['start end', 'end start'] })`
    - File contains the class `min-h-[100dvh]` (NOT `min-h-screen` or `100vh`)
    - File renders `.gradient-fade-bottom` and `.gradient-fade-top` overlays at z-index 5
    - File contains `.ambient-glow` wrapped in a `chapterIndex === 0` conditional (atmospheric layer first chapter only)
    - File imports `useReducedMotion` from `framer-motion` and gates `parallaxY`, `textOpacity`, `textY`, and paragraph stagger on `shouldReduce`
    - File imports `Image` from `next/image` and uses `fill` + `className="object-cover object-center"` for the photo backdrop
    - File contains `useInView(` invocation for the per-chapter active-flyTo callback (amount: 0.3) AND optionally also for the reduced-motion paragraph reveal
    - File exports a named `ChapterSection` function whose props include `chapter: StoryChapter`, `chapterIndex: number`, `photoUrl: string`, and an optional `onInView?: (chapterIndex: number) => void`
    - `npx tsc --noEmit` produces no errors for this file (when run from project root)
  </acceptance_criteria>
  <done>
    ChapterSection exists, type-checks, and exposes the exact scroll-linked motion contract from UI-SPEC. Plans 04-02 and 04-03 can import it without modification.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create ViewerNav — ghost floating nav with scroll-fade and Back-to-dashboard link</name>
  <files>src/components/viewer/ViewerNav.tsx</files>
  <read_first>
    - .planning/phases/04-cinematic-viewer/04-UI-SPEC.md (section: ViewerNav (Ghost Floating Nav) — full spec including scroll-fade thresholds and curve)
    - .planning/phases/04-cinematic-viewer/04-PATTERNS.md (section: ViewerNav — analog AppNav patterns, scroll-fade rAF pattern, back button pattern)
    - src/components/layout/AppNav.tsx (analog — wordmark, .nav-accent-line hairline, focus-ring pattern, sticky→fixed translation)
    - src/app/globals.css (.nav-accent-line definition — confirm present)
  </read_first>
  <action>
    Create `src/components/viewer/ViewerNav.tsx` as a `'use client'` component. Imports: `useEffect`, `useRef`, `useState` from react; `Link` from `next/link`; `ChevronLeft` from `lucide-react`.

    No props (the back link href is fixed to `/dashboard` per UI-SPEC).

    State: `const [navOpacity, setNavOpacity] = useState(1)`. Implement a `useEffect` running a `requestAnimationFrame` loop that reads `window.scrollY` and computes opacity using the EXACT scroll-fade curve from UI-SPEC §ViewerNav and §Motion Contract:
    - `scrollY <= 80`: `opacity = 1`
    - `scrollY >= 180`: `opacity = 0.08`
    - `80 < scrollY < 180`: linear remap `opacity = 1 - ((scrollY - 80) / 100) * 0.92`
    - Scroll-up reversal (when current `scrollY < previous scrollY` AND `scrollY > 80`): `opacity = 0.85` (NOT 1.0 — preserves ghost quality)
    Track `previous scrollY` in a ref or closure variable. On unmount, `cancelAnimationFrame`.

    Reduced motion: gate the scroll-fade effect on `window.matchMedia('(prefers-reduced-motion: reduce)').matches`. When reduced is true, still fade (opacity is a functional UX signal per UI-SPEC Reduced Motion table) but apply opacity change instantly (no CSS easing); when reduced is false, the inline-style opacity drives the value and the CSS transition handles smoothing.

    Render `<header className="fixed top-0 left-0 right-0 z-40 border-b border-[rgba(255,255,255,0.05)] bg-ink/80 backdrop-blur-xl" style={{ opacity: navOpacity, transition: 'opacity 300ms cubic-bezier(0.22, 1, 0.36, 1)' }}>`. Height: 64px desktop / 56px mobile (use Tailwind `h-14 md:h-16`).

    Inside, a flex container `<div className="mx-auto flex h-full max-w-[1280px] items-center justify-between px-4 sm:px-6 lg:px-8">`:
    - Left zone: `<Link href="/dashboard" aria-label="Back to your trips" className="inline-flex min-h-[44px] items-center gap-1 rounded-md px-3 py-2 font-sans text-sm font-medium text-parchment-400 transition-colors duration-300 ease-trailfilm hover:text-parchment focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-amber-accent focus-visible:ring-offset-2 focus-visible:ring-offset-ink active:scale-[0.97]">` containing `<ChevronLeft aria-hidden="true" className="h-5 w-5" strokeWidth={1.5} />` and `<span className="hidden md:inline">Back to trips</span>`.
    - Right zone: the wordmark `<span className="font-serif text-[19px] font-medium uppercase tracking-[0.04em] text-ink-50 transition-all duration-300 ease-trailfilm hover:tracking-[0.07em]">Trailfilm</span>` (carry forward AppNav wordmark, but render as `<span>` not `<Link>` — back button already handles navigation, wordmark is identity only). Position: right-aligned via the flex `justify-between`.

    After the inner flex container, render the amber hairline exactly as AppNav does: `<div aria-hidden className="nav-accent-line pointer-events-none absolute inset-x-0 bottom-0 mx-auto h-px w-1/2 opacity-60" />`.

    Export named `ViewerNav`.

    Do NOT add a user menu or a "New trip" button — this is a focused viewer nav. Do NOT use `sticky` — use `fixed` (UI-SPEC explicit). Do NOT use CSS `scroll-timeline` (insufficient browser support per UI-SPEC).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "viewer/ViewerNav" ; grep -c "ChevronLeft" src/components/viewer/ViewerNav.tsx ; grep -c "href=\"/dashboard\"" src/components/viewer/ViewerNav.tsx ; grep -c "fixed top-0" src/components/viewer/ViewerNav.tsx ; grep -c "z-40" src/components/viewer/ViewerNav.tsx ; grep -c "0.85\|0.85" src/components/viewer/ViewerNav.tsx ; grep -c "nav-accent-line" src/components/viewer/ViewerNav.tsx ; grep -c "Back to your trips" src/components/viewer/ViewerNav.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/viewer/ViewerNav.tsx` exists and starts with `'use client'`
    - File contains `fixed top-0` and `z-40` classes on the header element
    - File contains `bg-ink/80` and `backdrop-blur-xl`
    - File contains a `Link` (or `<a>`) with `href="/dashboard"`
    - File contains `ChevronLeft` import from `lucide-react`
    - File contains the literal string `Back to your trips` (mobile aria-label) AND `Back to trips` (desktop visible text)
    - File contains the literal numeric value `0.85` (scroll-up reversal opacity per UI-SPEC) AND `0.08` (deep-scroll target opacity)
    - File contains the thresholds `80` and `180` for scrollY fade boundaries
    - File contains `nav-accent-line` (amber hairline carry-forward)
    - File contains `requestAnimationFrame` (rAF-based scroll listener)
    - File contains a cleanup `cancelAnimationFrame` call in the useEffect return
    - File contains `transition: 'opacity 300ms cubic-bezier(0.22, 1, 0.36, 1)'` (CSS easing on opacity per UI-SPEC)
    - File exports a named `ViewerNav` function
    - `npx tsc --noEmit` produces no errors for this file
  </acceptance_criteria>
  <done>
    ViewerNav exists, type-checks, fades on scroll per UI-SPEC, and provides a reliable Back-to-dashboard exit path. Ready to be mounted by Plan 04-03 in place of the standard AppNav.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create ScrollProgress — fixed left-edge amber hairline driven by page scroll</name>
  <files>src/components/viewer/ScrollProgress.tsx</files>
  <read_first>
    - .planning/phases/04-cinematic-viewer/04-UI-SPEC.md (section: ScrollProgress — full spec including Option A/B, reduced-motion behavior)
    - .planning/phases/04-cinematic-viewer/04-PATTERNS.md (section: ScrollProgress — useReducedMotion gate carryover + Option A JSX)
    - src/components/trip/StoryChapterBlock.tsx (useReducedMotion gate pattern — line 15)
  </read_first>
  <action>
    Create `src/components/viewer/ScrollProgress.tsx` as a `'use client'` component. No props.

    Imports: `motion`, `useScroll`, `useReducedMotion` from `framer-motion`.

    Implementation: Use Option A from UI-SPEC (preferred). Inside the component:
    - `const shouldReduce = useReducedMotion()`
    - `const { scrollYProgress } = useScroll()` (page-level, NOT scoped — drives the whole-page progress)

    When `shouldReduce` is true: render a static decorative bar — `<div aria-hidden="true" className="fixed left-0 top-0 z-50 h-screen w-[3px] pointer-events-none" style={{ background: '#E5A663', opacity: 0.15, borderRadius: '0 2px 2px 0' }} />`. Full height, low opacity, no animation — communicates that story content exists below per UI-SPEC Reduced Motion table.

    When `shouldReduce` is false: render `<motion.div aria-hidden="true" className="fixed left-0 top-0 z-50 h-screen w-[3px] pointer-events-none" style={{ scaleY: scrollYProgress, transformOrigin: 'top', background: '#E5A663', borderRadius: '0 2px 2px 0' }} />`.

    Both branches: width = 3px (Tailwind `w-[3px]`), height = 100vh (`h-screen`), z-50, `position: fixed left: 0, top: 0`, `pointer-events: none` (pass-through), `border-radius: 0 2px 2px 0` (sharp left edge, soft right). Background must be the literal amber `#E5A663` (no opacity reduction in active state). `aria-hidden="true"` on both branches — decorative; screen readers do not need scroll position.

    Export named `ScrollProgress`.

    Do NOT add any box-shadow. Do NOT use the Tailwind `bg-amber-accent` class (UI-SPEC specifies the literal hex string in the style object so the value is unambiguous). Do NOT include the bar inside any layout that prevents it from being a fixed top-level element.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "viewer/ScrollProgress" ; grep -c "scaleY: scrollYProgress" src/components/viewer/ScrollProgress.tsx ; grep -c "#E5A663" src/components/viewer/ScrollProgress.tsx ; grep -c "useReducedMotion" src/components/viewer/ScrollProgress.tsx ; grep -c "fixed left-0 top-0" src/components/viewer/ScrollProgress.tsx ; grep -c "z-50" src/components/viewer/ScrollProgress.tsx ; grep -c "pointer-events-none" src/components/viewer/ScrollProgress.tsx ; grep -c "aria-hidden" src/components/viewer/ScrollProgress.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/viewer/ScrollProgress.tsx` exists and starts with `'use client'`
    - File imports `useScroll`, `useReducedMotion`, `motion` from `framer-motion`
    - File contains the literal string `scaleY: scrollYProgress` in the active-motion branch
    - File contains the literal hex string `#E5A663` (no opacity-reduced amber in the active branch)
    - File contains the literal number `0.15` for the reduced-motion opacity static branch
    - File contains class `fixed left-0 top-0` and `z-50` on the bar element
    - File contains `pointer-events-none` on the bar
    - File contains `aria-hidden="true"` (decorative — not announced to screen readers)
    - File contains `borderRadius: '0 2px 2px 0'`
    - File contains `transformOrigin: 'top'` in the active-motion branch (scaleY scales downward from top)
    - File exports a named `ScrollProgress` function
    - `npx tsc --noEmit` produces no errors for this file
  </acceptance_criteria>
  <done>
    ScrollProgress exists, type-checks, and produces a 3px amber left-edge bar that scales with `scrollYProgress`. Ready for Plan 04-03 to mount at the page root.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:
- `npx tsc --noEmit` from project root: passes for the three new files (and does not regress existing files)
- File listing: `src/components/viewer/ChapterSection.tsx`, `src/components/viewer/ViewerNav.tsx`, `src/components/viewer/ScrollProgress.tsx` all present
- Spec-locked transforms grep-verified in ChapterSection per acceptance criteria
- ViewerNav scroll-fade thresholds 80/180/0.08/0.85 all present
- ScrollProgress contains literal `#E5A663` amber hex (not Tailwind class) per UI-SPEC visual spec
</verification>

<success_criteria>
- ChapterSection ready to be rendered N times (one per chapter) by Plan 04-03
- ViewerNav ready to mount at page root in place of standard AppNav (suppression handled by Plan 04-03)
- ScrollProgress ready to mount at page root above all content
- No Mapbox dependency introduced in this plan — Plan 04-02 owns Mapbox in isolation
- No changes to `src/app/(app)/trip/[id]/page.tsx` or `src/app/(app)/layout.tsx` — Plan 04-03 owns those wiring changes
- All three components gate Framer Motion animations on `useReducedMotion()` per UI-SPEC Reduced Motion table
- Carries forward eyebrow format, brand easing, color tokens, focus-ring pattern, and amber hairline from Phase 1–3 patterns without modification
</success_criteria>

<output>
After completion, create `.planning/phases/04-cinematic-viewer/04-01-SUMMARY.md` listing files created, key motion constants implemented, and any deviations from UI-SPEC (there should be none).
</output>
